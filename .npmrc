# @vercel/analytics declares an optional Svelte peer chain that wants vite@8,
# which conflicts with the vite pulled in by Vitest. We use Next.js (not Svelte),
# so relax peer resolution for both `npm install` and CI's `npm ci`.
legacy-peer-deps=true
