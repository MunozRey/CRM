# Propel, Agent activity log

Append-only log of completed actions by each agent (Document/Backend/
Frontend/Cybersec/QA), per the team's closing routine: after any completed
action, log it here, commit, push. This is a log, not a task list, open
work lives in [`docs/BACKLOG.md`](docs/BACKLOG.md); prescriptive specs live
in [`docs/specs/`](docs/specs/); the maturity map lives in
[`docs/MASTER_PLAN.md`](docs/MASTER_PLAN.md); team roles/pipeline live in
[`docs/TEAM_PROCESS.md`](docs/TEAM_PROCESS.md).

Format: `- [YYYY-MM-DD HH:MM UTC] [Role]: <what happened>. Archivos modificados: a, b, c.`

---

- [2026-07-01 12:44 UTC] Document Agent: Audited README.md, ADR 0001, docs/sso-and-scim.md, docs/disaster-recovery.md, docs/design/propel/BRAND.md against the actual codebase, all three architecture docs still described the decommissioned pre-Supabase Express/Redis/PgBouncer stack, and README claimed SSO/SCIM/GDPR export as shipped when none existed yet. Corrected all five, added ADR 0002 and docs/BACKLOG.md. PR #18, merged. Archivos modificados: README.md, docs/adr/0001-tenant-isolation-and-rls.md, docs/adr/0002-rls-enforcement-post-migration.md, docs/design/propel/BRAND.md, docs/disaster-recovery.md, docs/sso-and-scim.md, docs/BACKLOG.md.
- [2026-07-01 12:54 UTC] Document Agent: Caught docs up on billing-tiers-v2 (#16), entitlements module (#17), and connectors-marketplace backend (#14) landing mid-audit; added docs/MASTER_PLAN.md (10-pillar maturity map). PR #20, merged. Archivos modificados: README.md, docs/BACKLOG.md, docs/MASTER_PLAN.md.
- [2026-07-01 13:04 UTC] Document Agent: Removed two BACKLOG items resolved by Backend/Frontend (#19 serviceClient→RLS fix, hardcoded 'free' slug fix); added docs/TEAM_PROCESS.md documenting the new team structure (Propel Orchestrator, Propel Cybersec, Propel QA) and the descriptive-docs-vs-prescriptive-specs split. PR #21, merged. Archivos modificados: README.md, docs/BACKLOG.md, docs/MASTER_PLAN.md, docs/adr/0002-rls-enforcement-post-migration.md, docs/TEAM_PROCESS.md.
- [2026-07-01 13:14 UTC] Document Agent: Wrote the first prescriptive spec under the new pipeline, docs/specs/contacts-deals-pipelines-limit-enforcement.md (DB-trigger design, PostgREST error-shape contract, edge cases, security-by-design section, QA test-case checklist), for MASTER_PLAN priority #1. Verified write paths/schema against real code before designing. Archivos modificados: docs/BACKLOG.md, docs/specs/contacts-deals-pipelines-limit-enforcement.md.
- [2026-07-01 13:22 UTC] Document Agent: Caught a squash-merge divergence, had kept building on a stale local branch instead of re-syncing from origin/master, which masked that Backend Agent shipped #22 (real GDPR /privacy backend: org export, per-subject DSAR export, anonymize-in-place erasure) in the meantime. Reset the branch onto current master, flipped every "GDPR not implemented" claim back to accurate (README's Compliance/API-overview/roadmap sections, MASTER_PLAN's Compliance pillar L2→L3), pruned BACKLOG's "recently resolved" traceability section per its own policy, and added this CHANGELOG.md. Archivos modificados: README.md, docs/BACKLOG.md, docs/MASTER_PLAN.md, docs/specs/contacts-deals-pipelines-limit-enforcement.md, CHANGELOG.md.
- [2026-07-01 13:26 UTC] Document Agent: Added the user's explicit merge-exclusivity rule to docs/TEAM_PROCESS.md for Backend/Frontend Agent, then confirmed with the user it also covers Document Agent, revoked today's earlier standing auto-merge-for-docs policy. Every agent now stops at push/PR; Propel Orchestrator (via the user) is the sole merge authority. Archivos modificados: docs/TEAM_PROCESS.md, docs/BACKLOG.md, CHANGELOG.md.
- [2026-07-01 13:32 UTC] Document Agent: Master had already moved again (#23 Slack/Zoom/webhooks requireFeature wiring, Frontend Phase 1 marketplace/webhooks/API-key gating, both items this session had flagged in BACKLOG, closed cleanly by Backend/Frontend agents themselves per the documented convention). Re-synced this branch onto that base, re-applied the GDPR + merge-rule work from the prior two entries (lost when the base moved out from under it), and updated MASTER_PLAN's Billing/Integrations pillars (Integrations L2→L3: entitlements-respecting connectors + at least one real dispatchOrgEvent wiring) to match. Stopping here per the new merge-exclusivity rule, PR pushed, not merged; queue re-checked, next item needs Propel Cybersec's review before any further Backend work can start on it. Archivos modificados: README.md, docs/BACKLOG.md, docs/MASTER_PLAN.md, docs/TEAM_PROCESS.md, docs/specs/contacts-deals-pipelines-limit-enforcement.md, CHANGELOG.md.
- [2026-07-01 13:35 UTC] Document Agent: Per explicit user instruction, switched from self-scheduled recurring re-audits to invoke-on-demand only, cancelled the session's recurring audit cron, confirmed `_orchestrator/estado.md` still doesn't exist (nothing to read orders from yet), and documented the new activation protocol (sync → read orders → execute in assigned area → close cycle) in docs/TEAM_PROCESS.md, noting the `main`-vs-`master` branch-name mismatch rather than silently guessing. Updated docs/BACKLOG.md's operating-process section to match. Going idle after this push, no further autonomous action until invoked. Archivos modificados: docs/TEAM_PROCESS.md, docs/BACKLOG.md, CHANGELOG.md.
- [2026-07-01 13:38 UTC] Document Agent: Invoked ("ponte a trabajar"). No new commits on master, no `_orchestrator/estado.md` yet, so picked up the next spec-writable BACKLOG item per MASTER_PLAN's priority order (onboarding plan-picker and legacy-plan transition were blocked on human decisions, not engineering-ready), wrote docs/specs/sso-oidc-scim.md, converting docs/sso-and-scim.md's design intent into a full spec (schema, endpoints, security-by-design, QA checklist), and surfaced an architecture question the original design never addressed: no subdomain-per-tenant means no URL implies "this login is for org X." Archivos modificados: docs/specs/sso-oidc-scim.md.
- [2026-07-01 14:05 UTC] Document Agent: PR #24 merged (by Orchestrator/human, confirming the new merge model works). Found `_orchestrator/estado.md` now exists with my task explicitly assigned: register product decisions A–D. Decision A (service_role exempt from the contacts/deals/pipelines cap) reversed my own spec's original recommendation, revised the SQL trigger, security-by-design section, and open-questions list in docs/specs/contacts-deals-pipelines-limit-enforcement.md accordingly, flagging the change prominently for Cybersec. Registered Decision D (legacy Free→Starter, decided but not built) in docs/BACKLOG.md/MASTER_PLAN.md. Also caught and corrected a stale note Cybersec's isolated session left in docs/BACKLOG.md (#26) claiming Document Agent's docs-only auto-merge exception was "still in force", it was fully revoked same-day before that note was written; cross-agent sessions don't share live state, so documented the correction rather than assuming malice or silently reverting. Reported status in `_orchestrator/estado.md`'s activity log per its own convention. Archivos modificados: docs/BACKLOG.md, docs/MASTER_PLAN.md, docs/specs/contacts-deals-pipelines-limit-enforcement.md, _orchestrator/estado.md, CHANGELOG.md.
- [2026-07-01 14:20 UTC] Orchestrator: Resolved this branch's merge conflict against master (PR #31), the conflict was entirely within `_orchestrator/estado.md`, which this branch had edited directly from a stale copy. Kept the Orchestrator's current version of that file and folded this branch's contribution into it by hand instead. Archivos modificados: _orchestrator/estado.md.
- [2026-07-01 14:35 UTC] Back End: Fixed C1 CRITICAL, the Stripe "Basil" API version was rejecting the webhook and breaking billing activation; pinned/aligned the API version in the stripe-webhook Edge Function so plan activation works again in edge/prod. PR #46, merged. Archivos modificados: supabase/functions/stripe-webhook/index.ts.
- [2026-07-01 14:50 UTC] QA: Ran a full guest-dogfooding bug sweep across the product and filed the report (2 Critical / 12 High / 9 Medium / 4 Low), which seeded the remediation queue for the rest of the day. PR #47, merged. Archivos modificados: docs/reviews/2026-07-01-full-bug-audit.md.
- [2026-07-01 15:10 UTC] Back End: Implemented the H3 branding handler and H4 email-tracking handler in the propel-api function and shared handlers. PR #48, merged. Archivos modificados: supabase/functions/_shared/handlers.ts, supabase/functions/propel-api/index.ts.
- [2026-07-01 15:30 UTC] Front End: Fixed H6 (inbox pagination) and H8 (analytics reporting false zeros). PR #49, merged. Archivos modificados: lib/supabase/analytics.ts, services/gmailService.ts.
- [2026-07-01 15:45 UTC] Orchestrator: Updated the board to register fusions #40–#49. PR #50, merged. Archivos modificados: _orchestrator/estado.md.
- [2026-07-01 16:05 UTC] Cybersec/Back End: Fixed H10, GDPR erasure was reporting success without actually banning the subject; erasure now bans in the privacy handler. PR #51, merged. Archivos modificados: supabase/functions/_shared/privacy.ts.
- [2026-07-01 16:25 UTC] DevOps: Added auto-deploy of Edge Functions on merge to master (CI workflow). PR #52, merged. Archivos modificados: .github/workflows/deploy.yml.
- [2026-07-01 16:40 UTC] DevOps: Extended auto-deploy to also apply migrations (db push) (PR #53), then reverted that step back to functions-only because the migration auto-apply was flaky (PR #57 revert). Net state: CI auto-deploys functions only. PRs #53 and #57, merged. Archivos modificados: .github/workflows/deploy.yml.
- [2026-07-01 17:00 UTC] Front End: Fixed H9 SYSTEMIC, optimistic writes now roll back and surface a toast.error on failed update/delete across 11 stores (previously silent). PR #54, merged. Archivos modificados: store/companiesStore.ts, store/contactsStore.ts, store/customFieldsStore.ts, store/distributionListsStore.ts, store/emailStore.ts, store/leadsStore.ts, store/notificationsStore.ts, store/productsStore.ts, store/sequencesStore.ts, store/templateStore.ts, store/viewsStore.ts.
- [2026-07-01 17:20 UTC] Back End: Fixed M7, fetchProfile admitted a user whose org was null on a transient error; auth now rejects that state. PR #55, merged. Archivos modificados: lib/supabase/auth.ts.
- [2026-07-01 17:40 UTC] Back End/Cybersec: Billing hardening, M1 access control, M2 402 handling, and M9 injection guard in entitlements/shared handlers. PR #56, merged. Archivos modificados: supabase/functions/_shared/entitlements.ts, supabase/functions/_shared/handlers.ts, supabase/migrations/20260701000026_limits_trigger_status.sql.
- [2026-07-01 18:05 UTC] Front End: Fixed M3, number/currency custom fields now accept decimals. PR #58, merged. Archivos modificados: components/shared/CustomFieldRenderer.tsx.
- [2026-07-01 18:20 UTC] Back End: Fixed M5, prevented double-booking in public-booking via a unique slot constraint and a 409 response. PR #59, merged. Archivos modificados: supabase/functions/public-booking/index.ts, supabase/migrations/20260701000027_bookings_slot_unique.sql.
- [2026-07-01 18:35 UTC] Front End: Fixed M6, assignee/creator was stored and shown as a raw UUID instead of a name. PR #60, merged. Archivos modificados: store/activitiesStore.ts, store/dealsStore.ts.
- [2026-07-01 18:55 UTC] Back End: Fixed M4, added a timeout guard on webhook/Slack dispatch that was hanging public endpoints. PR #61, merged. Archivos modificados: supabase/functions/_shared/dispatch.ts, supabase/functions/_shared/slack.ts.
- [2026-07-01 19:15 UTC] Front End/Back End: Fixed LOW items, L1 notifications badge in the Topbar and L3 past_due plan handling in the stripe-webhook (past_due → plan=null). PR #62, merged. Archivos modificados: components/layout/Topbar.tsx, supabase/functions/stripe-webhook/index.ts.
- [2026-07-01 19:40 UTC] Document: Wrote the prescriptive spec for the super-admin panel (H1). PR #63, merged. Archivos modificados: docs/specs/admin-panel.md.
- [2026-07-01 20:05 UTC] Back End: Implemented the super-admin panel /admin/* endpoints (H1) in propel-api, with the admin auth check in shared auth/handlers. PR #64, merged. Archivos modificados: supabase/functions/_shared/auth.ts, supabase/functions/_shared/handlers.ts, supabase/functions/propel-api/index.ts.
- [2026-07-01 20:30 UTC] Back End: Fixed CORS to send credentials only same-origin and corrected the MFA enrollment order (order factors by enrolled_at). PR #65, merged. Archivos modificados: lib/api.ts, lib/supabase/crmApi.ts.
- [2026-07-01 21:00 UTC] Back End: Fixed C2 DATA-LOSS, deleting an org member was cascading into deletion of the whole pipeline; scoped the delete in the crmApi layer. PR #66, merged. Archivos modificados: lib/supabase/crmApi.ts.
- [2026-07-02 09:15 UTC] QA: Ran the full product audit (4 parallel audits + dogfooding) and filed the consolidated report that drove the ux-flags remediation branch. PR #67, merged. Archivos modificados: docs/reviews/2026-07-02-full-product-audit.md.
- [2026-07-02 09:40 UTC] Back End: Fixed the broken create paths and added missing endpoints, deal creation (missing columns + phantom fields), company creation (strip virtual relations in the PostgREST layer), contact/deal create 400 (empty '' → UUID columns), activity created_by corruption + scoring-rule edit 404; added the /updates comments endpoint, the org-wide email-tracking stats endpoint, and admin org suspend/unsuspend; fixed the invite endpoint 500-on-success; applied migration 20260702000000_deals_missing_columns.sql. PR #67, merged. Archivos modificados: supabase/functions/propel-api/index.ts, supabase/functions/_shared/handlers.ts, supabase/functions/_shared/integrations.ts, supabase/migrations/20260702000000_deals_missing_columns.sql, lib/supabase/crmApi.ts.
- [2026-07-02 10:05 UTC] Front End: UX flags, stopped the false "created successfully" on failed writes and extended confirm-before-success to companies & activities; wired MFA/2FA to Supabase native TOTP (supabase.auth.mfa.*) via a new settings panel; added confirm-before-delete on 6 destructive actions plus confirm before Import-JSON wipes all data; i18n'd the ChoosePlan trial gate across 6 locales; added keyboard navigation to the shared Select (a11y); added an in-app 404 and an auth-form skeleton while the SPA loads; slimmed onboarding to name+workspace with a single canonical landing, added the trial onboarding funnel (route new/locked orgs to the plan picker), fixed the /signup dead-route and landing counters underselling to 0; hid the broken SMTP panel and impersonate button; added a tickets load-error state; fixed mislabeled form fields. PR #67, merged. Archivos modificados: components/ClientApp.tsx, components/auth/ProtectedRoute.tsx, components/activities/ActivityForm.tsx, components/deals/DealForm.tsx, components/import/CSVImport.tsx, components/settings/SettingsMfaPanel.tsx, components/ui/FullPageLoader.tsx, components/ui/SearchableSelect.tsx, app/[[...slug]]/page.tsx, views/ChoosePlan.tsx, views/Landing.tsx, views/OrgSetup.tsx, views/Tickets.tsx, views/inbox/InboxPage.tsx, views/settings/DataSection.tsx, views/settings/EmailSection.tsx, i18n/en.ts, i18n/es.ts, i18n/de.ts, i18n/fr.ts, i18n/it.ts, i18n/pt.ts, i18n/types.ts, store/authStore.ts.
- [2026-07-02 10:25 UTC] Document/Cybersec: Landed the RBAC/RLS role-aware proposal (NOT applied, for review; current RLS is org-scoped only via jwt_org_id() with no role enforcement) and the impersonation design proposal (awaiting review). PR #67, merged. Archivos modificados: docs/reviews/2026-07-02-rbac-rls-proposal.sql, docs/reviews/2026-07-02-impersonation-proposal.md.
- [2026-07-02 10:30 UTC] DevOps: Added the script to apply the branded auth-email templates to the remote Supabase project (not yet run against prod). PR #67, merged. Archivos modificados: scripts/apply-supabase-email-templates.sh.
- [2026-07-02 11:00 UTC] Front End: UX polish, clearer validation toasts and fixed remaining mislabeled fields on Automations, Products and SalesGoals, with locale strings updated. PR #68, merged. Archivos modificados: views/Automations.tsx, views/Products.tsx, views/SalesGoals.tsx, i18n/en.ts, i18n/es.ts, i18n/de.ts, i18n/fr.ts, i18n/it.ts, i18n/pt.ts, i18n/types.ts.
- [2026-07-02 11:30 UTC] Front End/Back End: Suspended-org enforcement, entitlements now carry a suspended flag through /billing/entitlements; the app locks a suspended org behind a terminal OrgSuspended screen; i18n'd the Admin view; added load-error states to Products/SalesGoals/Automations (and tickets); removed the dead /accept-invite route. PR #69, merged. Archivos modificados: supabase/functions/_shared/entitlements.ts, supabase/functions/_shared/handlers.ts, store/billingStore.ts, components/ClientApp.tsx, components/auth/ProtectedRoute.tsx, views/OrgSuspended.tsx, views/Admin.tsx, views/Automations.tsx, views/Products.tsx, views/SalesGoals.tsx, views/AcceptInvite.tsx, tests/entitlements.test.ts, i18n/en.ts, i18n/es.ts, i18n/de.ts, i18n/fr.ts, i18n/it.ts, i18n/pt.ts, i18n/types.ts.
- [2026-07-02 12:00 UTC] Test: Landed a real test suite, growing coverage from ~30 to ~144 tests, crmApi, store mutations, mappers, and API-contract tests. PR #70, merged. Archivos modificados: tests/crmApi.test.ts, tests/storeMutations.test.ts, tests/mappers.test.ts, tests/apiContract.test.ts.
- [2026-07-02 12:15 UTC] Document/Test: Refreshed every project .md against merged reality (#46–#70) and caught the Orchestrator board up, then landed the test follow-ups, exported and unit-tested the row mappers and expanded unit coverage by +43 (store rollback for #60/#58, crmApi edge cases, new-endpoint contracts). PR #71, merged. Archivos modificados: README.md, CHANGELOG.md, _orchestrator/estado.md, docs/BACKLOG.md, docs/MASTER_PLAN.md, docs/TEAM_PROCESS.md, docs/billing-tiers-plan-frontend.md, docs/disaster-recovery.md, docs/qa-log.md, docs/qa/test-plan-onboarding-trial.md, docs/reviews/2026-07-02-full-product-audit.md, docs/specs/admin-panel.md, docs/specs/contacts-deals-pipelines-limit-enforcement.md, docs/specs/sso-oidc-scim.md, supabase/email-templates/README.md, store/activitiesStore.ts, store/companiesStore.ts, store/dealsStore.ts, store/notificationsStore.ts, store/pipelinesStore.ts, tests/mappers.test.ts, tests/apiContractEndpoints.test.ts, tests/crmApiEdges.test.ts, tests/storeFixes.test.ts.
- [2026-07-02 12:30 UTC] Cybersec/Front End: Ran a 4-agent post-sweep adversarial audit (report filed) and fixed the client findings, H-1/H-2/L-1 nullifyEmptyStrings was nulling NOT NULL DEFAULT columns and breaking deal/activity create when a field was left blank (now omits them), and L-2 customFieldsStore was swallowing write failures (now rolls back and toasts). PR #72, merged. Archivos modificados: docs/reviews/2026-07-02-post-sweep-audit.md, lib/supabase/crmApi.ts, store/customFieldsStore.ts, tests/crmApiEdges.test.ts.
- [2026-07-02 13:00 UTC] Cybersec/Back End: Applied role-aware RBAC/RLS enforcement to prod (the old P0), migration 20260702010000 adds 3-tier role-aware policies across ~40 tables (Tier-1 write=non-viewer, Tier-2 write=owner/admin, Tier-3 append-only) plus a profiles BEFORE-UPDATE trigger (SECURITY INVOKER) that pins role/is_super_admin/organization_id/is_active on any authenticated update, closing the self-grant-super-admin escalation (C-1) and the org-only-no-role gap (C-2). Added a rollback script and a live RLS test harness. PR #73, merged. Archivos modificados: supabase/migrations/20260702010000_rbac_rls_role_enforcement.sql, docs/reviews/2026-07-02-rbac-rollback.sql, tests/rls/run.sh, docs/reviews/2026-07-02-post-sweep-audit.md, _orchestrator/estado.md.
- [2026-07-02 13:20 UTC] Back End: Audit follow-ups on prod, M-1 added the contacts.marketing_opt_in{,_at,_source} columns that were read but never existed (migration 20260702020000); M-2 public-api POST /contacts now enforces assertWithinLimit; L-3 /orgs/me/branding gates custom_domain behind the Enterprise feature; L-4 stripe-webhook re-fetches the live subscription per event to fix out-of-order status. PR #74, merged. Archivos modificados: supabase/migrations/20260702020000_contacts_marketing_optin.sql, supabase/functions/_shared/handlers.ts, supabase/functions/public-api/index.ts, supabase/functions/stripe-webhook/index.ts, docs/reviews/2026-07-02-post-sweep-audit.md.
- [2026-07-02 13:35 UTC] DevOps: Fixed deploy.yml, edge-function auto-deploy had been silently failing on every merge because SUPABASE_ACCESS_TOKEN was never set; it now no-ops green until the token is configured, and on deploy syncs edge-function runtime secrets from GitHub repo secrets to Supabase (skipping empty ones). GitHub is now the source of truth for edge secrets. PR #75, merged. Archivos modificados: .github/workflows/deploy.yml.
- [2026-07-02 13:45 UTC] DevOps: Added two workflows, backup.yml (nightly encrypted pg_dump that runs the existing scripts/backup-prod.sh) and vercel-env-sync.yml (manual, upserts NEXT_PUBLIC_* plus the service-role key into Vercel). PR #76, merged. Archivos modificados: .github/workflows/backup.yml, .github/workflows/vercel-env-sync.yml.
- [2026-07-02 14:00 UTC] Front End: Fixed the SearchableSelect dropdown panel width, long options (e.g. long industry names) no longer wrap into unreadable columns; the panel now grows to fit its content. PR #77, merged. Archivos modificados: components/ui/SearchableSelect.tsx.
- [2026-07-02 14:20 UTC] Front End: UX batch, defined the missing z-index tokens (dropdown/overlay/calendar) so any select rendered inside a modal no longer sits behind it (the New Company form looked broken); new sequences are now created INACTIVE instead of auto-live; booking-availability day toggles are now clear on/off switches; and standardized filter-select widths. PR #78, merged. Archivos modificados: tailwind.config.ts, views/BookingPages.tsx, views/Companies.tsx, views/Sequences.tsx.
- [2026-07-02 14:40 UTC] Front End: Made the sequence composer scroll (it was clipped) and standardized filter-select widths uniformly across every list view. PR #79, merged. Archivos modificados: features/sequences-flow/SequenceNodeInspector.tsx, views/Activities.tsx, views/Contacts.tsx, views/Leads.tsx, views/Products.tsx, views/deals/DealFilters.tsx.
- [2026-07-02 15:10 UTC] Front End: Big QA/UX sweep (4-agent audit → 23-agent fix), ~44 fixes across every view, Admin confirm dialogs on suspend/deactivate with a last-super-admin guard and per-row in-flight disabling; Products loading skeleton + addProduct error toast; Reports analytics error+retry; Booking status/error states; Leads owner/source default to the current user; await-before-success toasts in Activities/Calendar/Products; DataImport failure count; ChoosePlan/Billing empty/error states; removed the dead Lead-Ops panel; plus a11y label/aria fixes. PR #80, merged. Archivos modificados: components/auth/AuthSplit.tsx, components/deals/PipelineSelector.tsx, components/email/composer/ComposerFooter.tsx, components/email/composer/ComposerHeader.tsx, components/email/composer/EmailComposer.tsx, store/leadsStore.ts, store/productsStore.ts, store/sequencesStore.ts, views/Activities.tsx, views/Admin.tsx, views/BookingPages.tsx, views/Calendar.tsx, views/ChoosePlan.tsx, views/CompanyDetail.tsx, views/FollowUps.tsx, views/Leads.tsx, views/Login.tsx, views/Products.tsx, views/PublicBooking.tsx, views/Reports.tsx, views/Sequences.tsx, views/inbox/ThreadListPanel.tsx, views/settings/AdvancedSection.tsx, views/settings/BillingSection.tsx, views/settings/BrandingSection.tsx, views/settings/DataSection.tsx, views/settings/EmailSection.tsx.
- [2026-07-02 15:30 UTC] Front End/Test/DevOps: Summarized the LinkedIn industry list, the company Industry selector now offers the ~20 top-level groups (getIndustryGroupOptions) instead of the 434 granular ones, and new companies no longer silently default to 'Software Development'. Added component-test infra (jsdom + @testing-library on Vitest via tests/setup.dom.ts) with 30 component tests, plus an 'e2e' job in ci.yml that builds, serves, and runs the playwright-core crawl with the runner's Chrome, the crawl caught a real crash where billingStore hasFeature/isOverLimit read .features[key]/.usage off a malformed entitlements body (now guarded to fail open). PR #81, merged. Archivos modificados: .github/workflows/ci.yml, components/companies/CompanyForm.tsx, lib/industries.ts, package.json, package-lock.json, store/billingStore.ts, tests/components/CompanyForm.test.tsx, tests/components/DropdownMenu.test.tsx, tests/components/Inputs.test.tsx, tests/components/Modal.test.tsx, tests/components/SearchableSelect.test.tsx, tests/components/SelectWrapper.test.tsx, tests/e2e/crawl.mjs, tests/setup.dom.ts, views/Companies.tsx, vitest.config.ts.
- [2026-07-03 10:00 UTC] Perf/Backend: Response-time audit, RLS policies re-wrapped in (select …) for per-statement initPlan; edge JWT verify moved local; analytics date-window push-down; notifications payload bound; server-side list-param foundation in crmApi; bundle −650KB (industries JSON trim, recharts + non-default i18n code-split). PRs #84–#85, merged.
- [2026-07-03 11:00 UTC] Backend/Frontend: Org-wide settings now persist to Supabase (were localStorage-only): currency, lead SLA, tags, RBAC permission profiles, Google client id via organizations.settings->'app' and a new /orgs/me/settings route. PR #86, merged. Archivos modificados: supabase/functions/_shared/handlers.ts, supabase/functions/propel-api/index.ts, store/settingsStore.ts, hooks/useDataInit.ts, views/settings/BrandingSection.tsx, store/authStore.ts.
- [2026-07-03 13:00 UTC] Backend/Frontend: Functional audit (65 verified findings) fixed across contacts/companies/deals/leads/activities/email/team/booking/settings, false-success optimistic writes converted to settling promises, bulk rollback snapshots once, permission gating on viewer bulk controls, reusable booking links, lead score ordering, CSV import/export correctness, deals Won/Lost + automations execution, team seats/roles/deactivate, notification mutes persisted. PRs #87, #90–#92, #95, merged.
- [2026-07-03 13:30 UTC] Backend: Outbound email made reliable end-to-end. Built the missing /email/send + /smtp backend (Resend HTTP API, per-org SMTP via denomailer with AES-GCM passwords); real 5-attempt backoff on scheduled sends; server-side email runner (pg_cron → email-runner edge fn) so scheduled sends AND sequence steps dispatch with no browser open (Gmail via stored refresh token, SMTP, Resend). PRs #87–#89, merged.
- [2026-07-03 13:45 UTC] Backend: propel-api "Failed to fetch" root cause fixed, verify_jwt=false (the gateway rejected expired tokens with no CORS before the function ran); auth resolution moved inside the try so every error carries CORS; added GET /health, per-user rate limit, x-request-id + error logging. PR #91 (+ config), merged.
- [2026-07-03 14:30 UTC] Backend: **Public API v1**, the public-api edge function is now a full REST surface (contacts/companies/deals/leads/activities/tickets × list/read/create/update/delete) with limit/offset pagination, column filtering + sorting, per-resource scopes, plan-limit enforcement on create, a consistent error envelope, IP + per-key rate limits, and GET / , /openapi.json (OpenAPI 3.1), /me. See docs/api-public.md. PR #93, merged. Archivos modificados: supabase/functions/public-api/index.ts, supabase/functions/_shared/publicApiResources.ts, supabase/functions/_shared/publicApiOpenApi.ts.
- [2026-07-03 15:00 UTC] Backend/Frontend: RBAC permission profiles now ENFORCED, client hasPermission reads the org's persisted profiles; server jwt_has_permission() gates contacts/companies/deals/leads/activities/tickets insert/update/delete RLS. Backward-compatible (owner/admin always pass; no-profile roles keep the tier default). PR #98, merged.
- [2026-07-03 15:30 UTC] Frontend: Cleaner Propel sidebar lockup (self-contained dual-chevron mark + Schibsted Grotesk wordmark, no double box) and a segmented pill Deals view switcher. PRs #94, #97, merged.
- [2026-07-03 16:00 UTC] Frontend/Backend: **Tickets**, a drag-and-drop kanban board (status columns) + photo attachments (public ticket-attachments Storage bucket, org-scoped RLS; ticket detail drawer with thumbnail grid). PR #99, merged. Archivos modificados: supabase/migrations/20260703170000_ticket_attachments.sql, lib/supabase/storage.ts, store/ticketsStore.ts, views/Tickets.tsx.
- [2026-07-03 16:30 UTC] Backend/Frontend: **Super-admin impersonation** implemented (was proposed-not-built), custom_access_token_hook overrides org_id/app_role for a super-admin with an active impersonation_logs session; POST /admin/orgs/:id/impersonate + /admin/impersonate/exit; EnvironmentBanner Exit banner via the impersonating_org claim; Admin "Enter as this org" button un-hidden. PR #100, merged. Archivos modificados: supabase/migrations/20260703180000_impersonation.sql, supabase/functions/_shared/handlers.ts, components/layout/EnvironmentBanner.tsx, views/Admin.tsx.
- [2026-07-04 08:00 UTC] Backend/Frontend: Integrations marketplace (63 connectors, real logos + logo pipeline) and a genuinely functional agentic AI assistant (RLS-scoped tool loop). PRs #102–#103, merged.
- [2026-07-04 08:30 UTC] Backend: Full audit + hardening of both APIs, security, efficiency, dead-code removal; handlers.ts split into domain modules; hyper-detailed CLAUDE.md; ticket attachments isolated per tenant; SSRF gaps closed; 15 adversarial-QA findings fixed. PRs #104–#108, merged.
- [2026-07-04 09:30 UTC] Backend/Frontend: AI assistant made resilient and capable, graceful 429 degradation; Mistral (EU/GDPR) then NVIDIA providers; write-tool schema fixes; structured prompts + record grounding + more tools; compose assistant in the email composer (generate/rewrite tone+length); humanized voice (no em-dash) + per-org custom instructions; AI credits (100/500/2k/∞ per plan). PRs #109–#116, merged.
- [2026-07-04 10:30 UTC] Backend/Frontend: **Pricing v2**, new matrix (€39/79/149/299; 3/12/35/∞ seats), basic integrations open on all plans, Business+ AI governance gate, per-tenant limit overrides + resolver view, and 80%/100% proximity upgrade banners. PR #117, merged.
- [2026-07-04 11:00 UTC] Fix(AI): create_contact + create_deal tools (the agent was substituting create_task + hallucinating success), an honesty rule, and reply-in-user's-language. PR #119, merged.
- [2026-07-04 11:30 UTC] Feature: **Migrate from any CRM**, guided wizard (Pipedrive/HubSpot/Salesforce/Zoho/other), CSV *and* Excel (.xlsx, with Excel-serial date conversion), per-CRM column auto-mapping, dedup, relation resolution. PR #120, merged.
- [2026-07-04 12:00 UTC] AI: switched the primary model to Mistral (mistral-small-latest, reliable, NVIDIA 8b was returning empty) and added 9 tools (find_companies, pipeline_summary, my_open_items, create_company, update_contact, update_deal, convert_lead, schedule_meeting, complete_task). PR #123, merged.
- [2026-07-04 13:00 UTC] i18n: localized always-on chrome (impersonation banner, Admin) and the integrations marketplace catalog for es/pt/it (63 connectors; de/fr fall back to English pending). PRs #121, #124, merged.
- [2026-07-04 13:30 UTC] Frontend: interactive calendar create (prominent New event + day-prefill), safe dependency updates, sidebar Admin moved to the topbar + persisted collapse, dark-heatmap cell contrast. PRs #122, #125, #127, #130, merged.
- [2026-07-04 15:00 UTC] Perf/Fix: **the quality/architecture sweep**, Realtime stopped refetching the whole table on the user's own create/delete (the "page reloads on every action" feel); the CORS header gained Access-Control-Allow-Methods so edge DELETE/PATCH/PUT stop failing with "Failed to fetch" (couldn't delete booking links / cancel invitations); audit log loads on open; plan-limit errors are actionable instead of silent rollbacks; BookingPages delete/save optimistic. PRs #126, #128, #129, #131, merged.
- [2026-07-04 16:00 UTC] Feature: integration marketplace now renders **official simple-icons brand marks** (38 real logos, monogram fallback, no hand-drawn/fake marks). PR #133, merged. (Persistent app-shell refactor to kill the per-route remount/loading-flash, PR #132, in review.)
- [2026-07-04 20:15 UTC] Perf/Docs: landed the persistent app-shell refactor (#132, no per-route remount/loading-flash) and refreshed CHANGELOG + added the public-API reference (docs/api-public.md). PRs #132, #134, merged.
- [2026-07-04 21:00 UTC] Backend/Cybersec: **per-user data independence**, closed cross-user RLS leaks, added private views + per-record visibility (3-phase migration), so a user only sees their own records unless shared; plus an AI tool to seed realistic demo data on request. PRs #135, #136, merged. Archivos modificados: supabase/migrations/20260704200000_user_privacy_phase1.sql, supabase/migrations/20260704210000_user_privacy_phase2_views.sql, supabase/migrations/20260704220000_user_privacy_phase3_record_visibility.sql.
- [2026-07-04 22:00 UTC] Backend/Frontend: **plug-and-play chat integrations**, Discord, Telegram, Slack, Microsoft Teams (inbound webhooks) and Zapier/Make/n8n (outbound webhooks) are now real one-paste connectors; confirmed public bookings surface on the CRM calendar. PRs #137–#141, merged. Archivos modificados: supabase/migrations/20260704230000_chat_webhooks.sql, supabase/functions/_shared/dispatch.ts.
- [2026-07-05 09:00 UTC] DevOps: the nightly backup job now skips cleanly (green) when backup secrets aren't configured, instead of failing the run. PR #142, merged. Archivos modificados: .github/workflows/backup.yml, scripts/backup-prod.sh.
- [2026-07-06 10:15 UTC] Perf: prioritize critical data on app mount and defer secondary fetches; preconnect to the Supabase origin to cut first-request latency. PRs #143, #144, merged.
- [2026-07-07 11:00 UTC] Backend/Frontend: **real email open/click tracking**, the email-track edge function serves the tracking pixel + link redirects; hardened tracking rewrite, scheduled-send persistence, and sync window; CSP now allows the client-side Google APIs that were blocking Gmail inbox sync. PRs #145, #146, #148, #149, merged. Archivos modificados: supabase/functions/email-track/index.ts, vercel.json.
- [2026-07-07 12:00 UTC] Cybersec: security pass, closed an MFA-reload auth bypass, a role-escalation path, tenant/error leaks, a webhook signing oracle, and moved notifications to per-recipient/per-user RLS. PRs #147, #154, #158, merged. Archivos modificados: supabase/migrations/20260707130000_notifications_per_user_rls.sql.
- [2026-07-07 16:00 UTC] Perf/UX: killed reload-after-action and false-success saves, batched tracking-stats, added content-visibility on long card/list grids, and made Realtime refetch a silent background refresh (no skeleton flash). PRs #150–#153, merged.
- [2026-07-07 18:30 UTC] Backend: end-to-end email correctness pass, sequences, sending, inbox, and the scheduled-send lease; plus calendar timezone, deal clamp, and email-runner robustness fixes, and a scheduled-emails lease reaper. PRs #156, #157, merged. Archivos modificados: supabase/migrations/20260707120000_scheduled_emails_lease_reaper.sql.
- [2026-07-07 23:30 UTC] Frontend/Backend: **Inbox → Gmail parity** (waves 1–3), render received email as HTML; star/unstar + is:starred; forward; print; Gmail-style keyboard shortcuts; fetch/apply/remove Gmail labels; mark spam/important; Send & archive; and the `category:` search operator. Plus paginated Google sync, clearing the client Gmail session on disconnect, and day-panel Google events. PRs #159–#173, #176, merged. Archivos modificados: views/inbox/*.
- [2026-07-08 09:00 UTC] Frontend: **Calendar Google parity**, Day view with a full 24h grid + current-time indicator, and an Agenda/Schedule view. PRs #177, #178, merged. Archivos modificados: views/Calendar.tsx.
- [2026-07-08 10:00 UTC] Frontend: **new workspaces**, **Tasks / My Day** (edit + confirm-delete), **Duplicates** (duplicate-contact detection + merge), and **Explore** (ad-hoc report builder with month/source group-by and avg/min/max aggregations). PRs #179–#182, #195, #225, #227, merged. Archivos modificados: views/Tasks.tsx, views/Duplicates.tsx, views/Explore.tsx.
- [2026-07-08 10:30 UTC] Frontend: email composer hardening, draft persistence round-trips sender/signature/tracking, fixed state leaks + hidden recipients, and drag-drop/paste attachments with a 25MB cap. PRs #183–#185, merged. Archivos modificados: components/email/composer/*.
- [2026-07-08 12:30 UTC] Frontend: screen-by-screen QA/audit wiring across ~15 screens, design-token fixes, Tasks RBAC, unified activity-type colours, correct Forecast KPI labels + per-stage funnel colours, Sequences error+retry, shared PageHeader on Integrations. PRs #186–#189, merged.
- [2026-07-08 14:30 UTC] Frontend: **Leads** got a detail route + edit slide-over, bulk select (stage/convert/delete), and inline stage editing; notifications gained a per-type mute slide-over; contacts/companies export the current selection when one exists. PRs #190–#194, #196, merged. Archivos modificados: views/LeadDetail.tsx, views/Leads.tsx.
- [2026-07-08 16:00 UTC] Backend/Cybersec: **DB-constraint alignment + metric correctness**, never surface raw Postgres error text to clients; align lead lifecycle stages, lead status, activity types, and template categories with their DB CHECK constraints; correct won-date attribution, goal owner scoping, and Forecast won-YTD by close date. PRs #199–#203, merged.
- [2026-07-08 17:30 UTC] Frontend: audit-log free-text search + incremental paging; Custom Fields moved to its own Settings tab; team active-members search; and a probability-weighted subtotal per Kanban board column. PRs #208, #212, #213, #214, merged.
- [2026-07-08 18:15 UTC] Frontend: **Pipeline Timeline (Gantt)** polish, brand, theme-aware stage colours and a real left offset for the bars; Reports funnel keeps all stages and colours each segment. PRs #215–#217, merged. Archivos modificados: views/PipelineTimeline.tsx.
- [2026-07-08 22:00 UTC] Frontend/Backend: **Tickets** got a threaded discussion thread, contact/company linking, and subject/description editing; **Deals** capture a won/lost reason on close and allow manual card reordering within a stage; **Companies** gained an owner field + bulk assign. PRs #205, #218–#222, merged. Archivos modificados: supabase/migrations/20260708120000_company_owner.sql, supabase/migrations/20260708130000_deal_close_reason.sql, supabase/migrations/20260708140000_item_updates_ticket.sql, supabase/migrations/20260708150000_deal_position.sql.
- [2026-07-08 23:30 UTC] Backend/Frontend: **base-currency FX**, mixed-currency money totals now convert to the org base currency (with mixed-set flagging), including product prices in quotes; Reports can compare to the previous period; Admin "Export CSV" for Orgs/Users works client-side. PRs #209, #228, #230, #231–#233, merged. Archivos modificados: lib/currency.ts.
- [2026-07-09 07:00 UTC] Backend/Frontend: **automations go live**, rule actions fire online with a test-run, plus contact_created and activity_completed triggers; team/org-wide sales goals; user-defined email-template folders; and an org-wide sequence sending window (quiet hours) with a linear step editor. PRs #234–#238, #241, merged. Archivos modificados: views/Automations.tsx, views/SalesGoals.tsx, scripts (migrations 20260709000000_email_template_folder.sql).
- [2026-07-09 08:00 UTC] Backend/AI: persist branding legal/billing details and make the AI `pipeline_summary` tool currency-aware; runbook for the pending edge-function deploy. PRs #239, #240, merged.
- [2026-07-09 11:00 UTC] Frontend: **UI declutter**, collapsible sections on dense panels, form/toolbar cleanup (batches 1–3) including the booking editor, deactivated users hidden in all pickers, and an inline "+ Create" contact/company from the searchable deal/activity pickers. PRs #242–#246, merged. Archivos modificados: components/ui/SearchableSelect.tsx.
- [2026-07-09 11:15 UTC] Frontend: **Inbox thread snooze**, hide a Gmail thread until a chosen wake time. PR #247, merged. Archivos modificados: views/inbox/*.
- [2026-07-09 13:30 UTC] DevOps: **demo seed**, `scripts/seed-demo.mjs` builds the "Propel HQ" org (~150 companies / 400 contacts / 50 deals / 100 leads + activities, tickets, products, goals, templates, sequences) with owner-move + stale-user cleanup, plus a manual CI workflow to run it. PRs #248–#250, merged. Archivos modificados: scripts/seed-demo.mjs, .github/workflows/.
- [2026-07-10 22:00 UTC] Full-stack: **Feature-parity + polish push (#272–#296).** View consolidation, Tasks folded into an Activities tab and Reports/Forecast/Manager merged into one `/analytics` hub (old routes redirect). Command palette leveled up (quick-create, full nav/entity + Leads search, Recently-viewed, `g`-navigation + `?` overlay). CSV export added to Leads/Deals (shared RFC-4180 helper); sequence analytics (Metrics funnel + per-sequence reply-rate); bulk enroll-in-sequence + on ContactDetail; list/detail parity (company quick-create, Leads bulk-assign, Mine/All on Companies+Leads, bulk-tag, LeadDetail updates+timeline, Deals save-view); Calendar form i18n; CX fixes (notification deep-links, selection-aware Deals export, styled ConfirmDialog). **Duplicate MERGE shipped** (was a detection-only stub): migration `20260710150000` (contacts/companies `is_archived`+`merged_into_id`), transactional `merge_records`/`merge_records_preview` RPCs (org-scoped, manager+, re-point FKs → survivor, archive losers reversibly, audit-logged), `MergeDuplicatesDialog` UI; internal + public-api lists filter `is_archived`. Added `leads:export`/`deals:export` perms. Test suite 281→320 (recents, bulkUpdate fn-form, nav sanitizer, CSV, RBAC, sequence funnel, deal-rot). Docs reconciled (README/CLAUDE/BACKLOG/MASTER_PLAN). Archivos modificados: views/*, components/*, features/sequences-flow/*, services/mergeService.ts, supabase/migrations/20260710150000_merge_records.sql, supabase/functions/_shared/publicApiResources.ts, utils/csv.ts, i18n/*, tests/*, README.md, CLAUDE.md, docs/BACKLOG.md, docs/MASTER_PLAN.md.
- [2026-07-11 06:00 UTC] Cybersec/Backend: **Heavy pentest + remediation (#298–#308, all live-validated vs prod).** Parallel auditor/pentester agents plus real curl exploits against prod closed every CRITICAL/HIGH. **CRITICAL:** (1) self-signup platform takeover, `profiles` was `FOR ALL` and the privilege-pin trigger only fired on UPDATE, so any user could DELETE their own profile then re-INSERT `is_super_admin=true`; split into explicit `rls_profiles_read`/`_update`, revoked client INSERT/DELETE (validated → 403). (2) tenant identity wipe (viewer could DELETE teammates), same fix. (3) monetization bypass, `subscriptions` admin-writable let a tenant self-grant Enterprise; writes are service_role only now (→ 403). **HIGH:** bulk-insert bypass of the plan-limit trigger (added `assert_org_within_limit_stmt` AFTER-STATEMENT trigger with transition table); tenant self-unsuspend/upgrade (pinned `organizations.status`/`plan` + `jwt_is_admin()` impersonation-aware); deactivation was cosmetic (now bans the GoTrue user); impersonation write-paths (`assertNotImpersonating` on every serviceClient() write); shared-Resend relay gated on subscription. **MEDIUM/LOW:** MFA aal2 enforced in the JWT hook, webhook SSRF fail-closed, webhook per-org cap, pipelines cross-org userId check, ai/execute role re-check, CSV export formula-injection guard, public-form input caps. Migrations 20260711000000–060000. Archivos modificados: supabase/migrations/2026071100*.sql–2026071106*.sql, supabase/functions/_shared/{auth.ts,ssrf.ts,emailOutbound.ts,integrations.ts,aiAgent.ts,handlers/*}, supabase/functions/public-forms/index.ts, supabase/functions/public-booking/index.ts, utils/csv.ts, views/{Reports,Admin,Explore}.tsx, lib/supabase/storage.ts.
- [2026-07-11 14:00 UTC] Full-stack: **Full performance audit + fixes for scale (#309–#318).** 4-dimension parallel audit (re-render / fetching / DB-concurrency / bundle). **Re-render (the "everything re-renders on every action" complaint):** extracted `React.memo` row components with primitive/ref-stable props + `useCallback` handlers for all four entity lists (`ContactRow`/`CompanyRow`/`LeadRow`/`DealListRow`) and the kanban `DealCard`, so a realtime patch re-renders one row not the whole list; `useDeferredValue(search)`; `LeadRow` gets only its own score-insight. **Scale/security:** Realtime cross-tenant WIRE leak + fan-out fixed, the schema-wide `postgres_changes` sub had no server-side filter, so every tenant's row payloads reached every client's socket; now per-table `filter: organization_id=eq.<org>`; incremental store apply (no full-table refetch on a remote edit); `audit_log` refetch bounded (`?limit=500`). **Payload/latency:** Sentry (185KB gzip dead code, never `init`-ed) moved off the critical path (−38% initial JS); contacts list payload −27% + leads −18% via `?select=` column projection (with `fetchById` full-row reload on detail); `/preferences/me` deduped 3→1 per login; redundant whole-table `fetchLeads` guarded; O(1) company-name Map. **DB:** dropped 4 duplicate indexes, added 6 missing (merge/owner-cascade/AI-search). **Capacity reality (verified):** org is Supabase **free** tier (nano) → ~200-concurrent hard cap (Realtime), 500MB data, 5GB/mo egress; 1000 concurrent needs Pro + a compute add-on (infra, not code). Deferred (need browser QA / are architectural): table virtualization, maximal `notes` trim, true server-side row pagination. Archivos modificados: lib/realtimeSubscriptions.ts, lib/preferences.ts, lib/supabase/crmApi.ts, components/{contacts/ContactRow,companies/CompanyRow,leads/LeadRow,deals/DealListRow,deals/DealCard,layout/ErrorBoundary}.tsx, store/{contacts,leads,navigationPrefs,notifications,onboarding,audit}Store.ts, views/{Contacts,Companies,Leads,ContactDetail,LeadDetail,ManagerDashboard,deals/*}.tsx, supabase/migrations/20260711070000_drop_duplicate_indexes.sql, tests/realtimeIncremental.test.ts.
- [2026-07-12 05:40 UTC] Document/Test: **Two follow-up changes landed on master.** (#320) Added `docs/RELEASES.md`, human-readable, versioned release notes (SemVer-ish, "The One Where…" codenames) that map the real 2026-07-01…07-11 history into 10 milestone releases (v1.0.0 → v2.6.0); complements this append-only engineering log. (#321) Expanded the Vitest suite by **+403 tests across 19 new files** covering previously-untested pure logic, `utils/` (dealHealth, duplicateDetection, followUpEngine, formatters, emailMergeFields, emailPlainFormatting, quickCreate, outboundEmailIdentity, a11y, managerDashboardMetrics, permissionProfiles) and `lib/` (csvParse, entityListFilters, emailTracking, securePassword, industries, supabaseHelpers, appIdentity, brandingAccent); no source changes. Suite is now **50 files / 734 passing** (tsc + lint:ci clean). Archivos modificados: docs/RELEASES.md, tests/utils/*, tests/lib/*.
- [2026-07-12 06:00 UTC] Document: Reconciled the living docs with #320/#321, refreshed every stale test count (README badge + `331`/`31 files` in three spots; CLAUDE.md & MASTER_PLAN `~320`/`29 files` → **734 tests / 50 files**) and registered `docs/RELEASES.md` in the doc inventory (README doc-link list + CLAUDE.md repo layout). Archivos modificados: README.md, CLAUDE.md, docs/MASTER_PLAN.md, CHANGELOG.md.
- [2026-07-12 06:20 UTC] Document: Repositioned the README from an open-source-style project to a **proprietary commercial product**. Added a top-of-file proprietary/confidential notice, a new **License & Ownership** section (© 2026 Propel, all rights reserved, no license granted, hosted-SaaS-only distribution, no self-host/community/free tier), flipped the license badge Commercial→Proprietary (linked to the new section + TOC entry), and reframed **Quick Start** as internal engineering setup on a private codebase (not a public "get started" guide), plus a copyright footer. Archivos modificados: README.md, CHANGELOG.md.
- [2026-07-12 07:10 UTC] Document/Legal: **Extensive legal & privacy review + drafted the full document suite** under `docs/legal/` (14 files, ~3.7k lines) now that Propel is a commercial product selling to EU businesses. Added: a gap-analysis **review** (LEGAL-PRIVACY-REVIEW.md, findings, risks, P0/P1/P2 actions), **Privacy Policy**, **Terms of Service**, GDPR Art. 28 **DPA** (+ Annexes I–III), **Subprocessor Register**, **Security Measures (TOMs)**, **Cookie Policy**, **Acceptable Use Policy**, **Data Retention Policy**, **DSAR Procedure**, **Data Breach Response Plan** (Art. 33/34), **SLA**, and a Spanish **Aviso Legal** (LSSI-CE art. 10). All grounded in the product as built (controller/processor split, EU residency + SCCs, real DSAR export/erase endpoints incl. the free-text-scrub limitation), all with `[PLACEHOLDER]` corporate tokens and a "draft, not legal advice, review by counsel" disclaimer. Linked from the README doc index. Docs-only; no source/i18n/config touched. Archivos modificados: docs/legal/*, README.md, CHANGELOG.md.
- [2026-07-12 08:15 UTC] Backend/Frontend: **AI Meeting Notes**, new feature inspired by the MIT-licensed `Zackriya-Solutions/meetily` meeting assistant, reimplemented natively on Propel's AI + activities stack (no meetily code copied; the desktop Rust/Whisper recorder was deliberately left out as a poor fit for a hosted web CRM). Paste/upload a transcript (`.txt`/`.vtt`/`.srt`) → new edge endpoint **`POST /ai/meeting-notes`** turns it into a structured **summary + key points + decisions + action items + next step + sentiment** (same AI governance / token-cap / AI-credit gating as the other `/ai/*` calls; JSON-only prompt + defensive `parseMeetingNotes`), then the client saves it as a **meeting activity + tasks** (from the selected action items) on the linked contact/deal via RLS-scoped PostgREST. Entry points: a `MeetingNotesButton` on Contact/Deal detail (auto-hides when AI is off) + a command-palette action; global `MeetingNotesDialog` mounted in `Layout`. i18n across all 6 locales; pure transcript helpers with 9 unit tests. **Zoom / Google-Meet transcript ingestion designed (Phase 2)** in `docs/specs/ai-meeting-notes.md`. Suite 51 files / 743 tests; `tsc` + `lint:ci` + `build` green. Archivos modificados: supabase/functions/_shared/handlers/ai.ts, store/aiStore.ts, components/ai/{MeetingNotesDialog,MeetingNotesButton}.tsx, components/layout/{Layout,CommandPalette}.tsx, views/ContactDetail.tsx, views/deals/DealsPage.tsx, utils/meetingTranscript.ts, i18n/*, tests/utils/meetingTranscript.test.ts, docs/specs/ai-meeting-notes.md.
- [2026-07-12 09:30 UTC] Frontend/Legal: **Wired the drafted legal docs into the public site.** Expanded the existing `/privacy`, `/terms`, `/gdpr` pages into comprehensive public policies (from `docs/legal/*`) and added new pages **`/cookies`, `/subprocessors`, `/acceptable-use`, and `/aviso-legal`** (Spanish), all in the marketing chrome. Added an **LSSI-CE / ePrivacy cookie-consent banner** (`CookieConsent`, essential vs optional, choice stored in localStorage, reject as easy as accept) mounted on the landing and all marketing pages, and expanded the footer Legal column. Corporate `[PLACEHOLDER]` tokens still to be completed by counsel; each page keeps the "not legal advice" note. Also corrected the AI-Meeting-Notes spec's Phase-2 feasibility (Zoom has **no** OAuth today, webhook-secret only; Google Meet needs a new restricted scope + Google app verification). All 13 marketing pages prerender; `tsc` + `lint:ci` + `build` green. Archivos modificados: app/{privacy,terms,gdpr,cookies,subprocessors,acceptable-use,aviso-legal}/page.tsx, components/marketing/{CookieConsent.tsx,chrome.tsx,PropelLanding.tsx}, docs/specs/ai-meeting-notes.md, CHANGELOG.md.
- [2026-07-12 10:15 UTC] Backend/Frontend: **AI Meeting Notes, Phase 2 scaffolding (Google Meet, gated OFF).** Added the provider-import plumbing: edge `GET /ai/meeting-notes/sources` + `POST /ai/meeting-notes/import`, exported `googleMeetConnected` / `listMeetTranscripts` / `fetchMeetTranscript` helpers in `_shared/google.ts` (reusing `getValidAccessToken`), and a source-tab UI in the Meeting Notes dialog (Paste/Upload · Google Meet · Zoom). **Deliberately inert:** Meet transcripts need the `meetings.space.readonly` scope which is **not** added to the live OAuth bundle, a restricted scope needs Google app verification + re-consent, and adding it carelessly can disrupt the working Gmail/Calendar connection. Until the scope is granted, `googleMeetConnected` is false, the helpers short-circuit **without calling Google**, and the UI shows a "coming soon / connect" state, zero risk to existing integrations. Zoom stays disconnected (no OAuth today). i18n for the source tabs across 6 locales. Feasibility + prerequisites are in `docs/specs/ai-meeting-notes.md`. `tsc` + `lint:ci` + `build` + 743 tests green (edge is typechecked at deploy). Archivos modificados: supabase/functions/_shared/{google.ts,handlers/ai.ts}, store/aiStore.ts, components/ai/MeetingNotesDialog.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, CHANGELOG.md.
- [2026-07-12 11:00 UTC] Frontend: **Two UX fixes.** (1) **Booking editor overflow**, the per-day availability row now wraps (`flex-wrap`), so the start/end time inputs that appear when a day is toggled **on** no longer push the row past the card edge (fixes the "day toggle sticks out when active" report). (2) **Connections reachable from Settings**, the connector marketplace + provider OAuth (Gmail/Calendar/Slack/Zoom + 63 connectors) lived only at an orphaned `/settings/integrations` route with no link from the Settings UI, so users couldn't find it. Added a **"Connections" tab** in Settings that renders `IntegrationsMarketplace` (the existing "API & capture" tab, API keys + lead-capture forms, is unchanged). i18n `settings.tabConnections` across all 6 locales. `tsc` + `lint:ci` + `build` + 743 tests green. Archivos modificados: views/BookingPages.tsx, views/settings/SettingsPage.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, CHANGELOG.md.
- [2026-07-12 20:41 UTC] Frontend/QA: **Integrations marketplace, honest status + 2 more live connectors, plus a big E2E coverage expansion.** (1) Every connector card now carries a real state badge, **Available now** (self-service, connects today), **Setup required** (wired UI but blocked on missing server credentials, e.g. the Gmail/Calendar/Contacts trio until `GOOGLE_CLIENT_ID/SECRET` are set), or **On the roadmap** (catalog-only), with a **Todas | Disponibles** filter and the grid split into a prominent "Available now" section over a muted "roadmap" one, so the showroom stops implying everything connects. (2) Wired **two more real connectors**, **Pipedream** and a generic **Custom Webhook**, riding the existing signed-JSON `/webhook-subscriptions` backend (HMAC, SSRF guard, retries, outbox already live), taking the live set to 13. (3) **Expanded the E2E crawl**: `/integrations` marketplace interactions (search, Available filter, open a wired vs a roadmap connector), a full **Settings-tab sweep** (all 16 tabs), six more create-modal flows, and four more marketing routes. The tab sweep **caught a real bug**: `SettingsWebhooksPanel` crashed the whole Settings page (`rows.map is not a function`) when the subscriptions endpoint returned a non-array body, `?? []` does not catch `{}`. Fixed with an `Array.isArray` guard (matching `WebhookConnectCard`) and locked in with a regression test. `tsc` + `lint:ci` + `build` + **750 tests** + `npm run e2e` green. Archivos modificados: lib/integrationsCatalog.ts, components/integrations/{IntegrationsMarketplace,WebhookConnectCard}.tsx, components/settings/SettingsWebhooksPanel.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/components/{IntegrationsMarketplace,SettingsWebhooksPanel}.test.tsx, tests/e2e/crawl.mjs, CHANGELOG.md.
- [2026-07-12 21:14 UTC] QA: **E2E, the four inline create flows now hard-validate (no more soft warnings).** Follow-up to the marketplace/E2E PR: split the crawl's create-flow check into modal creators (expect a `[role=dialog]`) and inline creators, **leads** (quick-add panel), **goals** (inline form), **sequences** (editor pane + toast), **templates** (editor). Each inline creator now asserts the click actually reveals a form/editor, navigates, or fires a toast, promoting the previous "create modal did not open" **warnings into real failures**. Fixed the templates matcher (a generic "Add" button was being clicked before "New Template") and taught the mocked backend to echo a created row for inserts so the templates editor opens. `npm run e2e` green (only the known login-redirect flake remains as a non-fatal warning). Archivos modificados: tests/e2e/crawl.mjs, CHANGELOG.md.
- [2026-07-12 21:50 UTC] QA/Cross-cutting: **Test-driven bug-hunt sweep, 15 real bugs found & fixed, each with a regression test.** Ran five parallel adversarial hunts (stores, data-access/mappers, pure utils, component rendering, auth/permissions), independently verified every finding, and fixed the confirmed ones. **Security:** `safeHref` accepted a NUL-obfuscated `javascript:` URL (control chars now rejected, stored-XSS sink on contact/company/ticket links); `formatPlainToHtml` did not escape `"`, letting a quote in a markdown URL inject `onerror=`/`onmouseover=` into outbound email HTML; `safeSupabaseMessage` leaked PostgREST `PGRST204` schema-cache errors (table/column names) raw to clients. **Data/correctness:** `dealsStore.moveDeal` re-fired won/lost notifications + close automations and reset `stageChangedAt` on a **same-stage** move (reachable via bulk "set stage", polluted reports + duplicate automations); `contactsStore.bulkDelete` resurrected already-deleted rows on a partial failure (Promise.all→allSettled, revert-only-failed); `salesReps` dropped every deal with a null `owner_id` + name-based `assigned_to` from per-rep reports; analytics month-bucketing mixed UTC keys with local bucket labels (deals mis-bucketed near month edges off-UTC). **Robustness:** `PublicLeadForm` white-screened on a malformed form config (public, error-boundary-less lead-capture route); TSV import collapsed every row into one column (`parseCSV` now splits on tab); `csvCell` mangled negative numbers into text; `getInitials` broke on stray whitespace; `parseDealValue` truncated multi-comma values; two marketplace `?? []` sites hardened to `Array.isArray`. Added 18 regression tests (each verified to fail without its fix). `tsc` + `lint:ci` + **768 tests** + `build` + `npm run e2e` green. Documented but deliberately NOT auto-fixed (auth-sensitive / test-enshrined, flagged for a decision): a stale-session `expiresAt` bounce + its dead-code Layout logout, stale-persisted permission profiles hiding new permissions, `normalizeRole` unknown-role fail-open, and `nullifyEmptyStrings` not clearing NOT-NULL-default text on update. Archivos modificados: utils/{safeHref,emailPlainFormatting,csv,formatters}.ts, lib/{csvParse,readSpreadsheet,csvRowSchemas}.ts, lib/supabase/{analytics,crmApi}.ts, store/{dealsStore,contactsStore,notificationsStore}.ts, views/PublicLeadForm.tsx, components/integrations/{WebhookConnectCard,IntegrationsMarketplace}.tsx, tests/utils/bugHuntFixes.test.ts, tests/lib/bugHuntMappers.test.ts, tests/storeBugHuntFixes.test.ts, tests/components/PublicLeadForm.test.tsx, CHANGELOG.md.
- [2026-07-12 22:14 UTC] QA: **Broad unit-coverage expansion, the suite goes 768 → 1317 tests (+549), and caught 2 more bugs.** Ran six parallel test-authoring passes over the previously-untested surface, each iterating to green: **hooks** (46, useLocalStorage/useSearch/useImpersonation/useDateLocale/usePresence), **services** (86, mergeService, bookingService, googleIntegrationService, gmailTokenRefresh, the Resend/SMTP/Gmail providers), **stores** (409 across 17 stores, billing/customFields/views/nav-prefs/thread-snooze/dashboard/ui/onboarding, products/goals/tickets/distribution-lists/templates/attachments, and the complex sequences/settings/email stores), and **pure lib** (125, crmMigrationImport, integrationsCatalogI18n, chartTheme, theme, uxMetrics, dateFnsLocale, localWrites, envChannel, routePreload, traceFetch). Focus areas: optimistic-write + rollback paths, response-shape/`Array.isArray` guards, entitlement-gating boundaries, and malformed-input resilience. **Two bugs found & fixed with regression tests:** `getCurrentOrganizationId` stripped wrapping quotes before trimming (a whitespace+quote-wrapped org id kept its quotes in the OAuth payload); `settingsStore.resetToDefaults` omitted `recordVisibility`/`fxRates`/`sequenceWindow` from its server reset patch, so `loadServerSettings` re-hydrated the old values and the reset silently un-did itself on reload. Also fixed one test-only tsc error (`owner` is not a `UserRole` key). `tsc` + `lint:ci` + **1317 tests** + `build` + `npm run e2e` green. Archivos modificados: tests/hooks/*, tests/services/*, tests/stores/*, tests/lib/* (40 new files), services/googleIntegrationService.ts, store/settingsStore.ts, CHANGELOG.md.
- [2026-07-13 06:09 UTC] Document: **Refreshed the living docs to the current state.** Updated every stale test/coverage/connector count: README badge + prose (734/50 → **~1,990 tests / 262 files**, added **~74% coverage** via `npm run coverage`; **63 → 38 connectors** with the honest Available/Setup/Roadmap status model + the 13 wired ones incl. Pipedream/Custom Webhook), CLAUDE.md §6 (marketplace) + §7 (test/coverage commands + the views/components/stores/hooks/services test layout), docs/MASTER_PLAN.md (test-count evidence), docs/integration-logos.md (38 connectors), and added a witty **v2.7.0 "The One Where We Tested Absolutely Everything"** entry to docs/RELEASES.md. Docs-only, no source touched. Archivos modificados: README.md, CLAUDE.md, docs/MASTER_PLAN.md, docs/integration-logos.md, docs/RELEASES.md, CHANGELOG.md.
- [2026-07-13 11:57 UTC] Document: **Final living-docs refresh to the post-coverage-wave numbers.** After the store/service/function coverage passes (#337, #338) landed the residual, the suite settled at **~2,500 tests across 288 files at ~80% line/statement coverage** (up from the ~1,990/262/74% documented earlier in the day). Bumped every stale count to the final figures: README badge (`2502 · 80% cov`) + prose, CLAUDE.md §7 (`~2500 tests across 288 files`, `~80% lines/statements`), docs/MASTER_PLAN.md (`~2,500 / 288 files`, `~80% line coverage`, progression `734/50 → 2502/288`), and the docs/RELEASES.md v2.7.0 entry (`~80% line coverage, up from 16%`, `734 → ~2,500 tests`). Docs-only, no source touched. Archivos modificados: README.md, CLAUDE.md, docs/MASTER_PLAN.md, docs/RELEASES.md, CHANGELOG.md.
- [2026-07-13 12:14 UTC] Backend/Frontend: **Fixed the stale-session bounce to /login (the top flagged finding).** On the Supabase auth path the client silently auto-refreshes the access token in the background, but nothing synced the new expiry back into the store, so `session.expiresAt` froze at the original ~1h value and `isAuthenticated()` (used by `ProtectedRoute`/`ClientApp`) kicked a still-valid session to `/login` after an hour of continuous use. `initAuth` now subscribes to `supabase.auth.onAuthStateChange` and (a) mirrors the real token lifetime into `session.expiresAt` on **TOKEN_REFRESHED/SIGNED_IN/USER_UPDATED** (guarding the INITIAL_SESSION race, never fabricates a session for an unhydrated user), and (b) clears the store on a genuine **SIGNED_OUT** (revoked refresh token / other-tab logout); the subscription is torn down via the effect cleanup already wired in `ClientApp`. Also fixed the **dead code** it exposed: `Layout`'s 60s expiry check guarded on `isAuthenticated()` (which itself requires `expiresAt > now`), making its logout branch unreachable, re-gated on `currentUser` so it works as a real safety net for a tab suspended past the refresh window (logout + toast + redirect). +8 regression tests (token-refresh sync, SIGNED_OUT clear, race guard, unsubscribe cleanup; Layout expired/live/logged-out). `tsc` + `lint:ci` + **2509 tests** + `build` green. Archivos modificados: store/authStore.ts, components/layout/Layout.tsx, tests/stores/authStore.test.ts, tests/components/layout/Layout.test.tsx, CHANGELOG.md.
- [2026-07-13 12:35 UTC] Cybersec/Backend: **Fixed a cross-tenant account-ban vector in `POST /privacy/erase` (GDPR erasure).** Surfaced by a parallel security audit. The `profile` erase path bans the auth account via a service-role `auth.admin.updateUserById(subjectId, { ban_duration })` that trusts the client-supplied `subjectId` and is **not** org-scoped; the `gdpr_erase_subject` RPC's `WHERE organization_id = p_org_id` clause failed **open** (touched 0 rows and returned success on a cross-org miss, never raising). Net effect: any owner/admin of any tenant could POST another org's user UUID and permanently disable that account platform-wide (cross-tenant DoS on any user, incl. potentially super-admins). Fix, defense-in-depth: (1) **edge** `privacy.ts` now verifies the profile subject is a member of the caller's org (`profiles` lookup scoped by `organization_id`) BEFORE the RPC/ban, returning 404 otherwise, ships via the propel-api function auto-deploy; (2) **migration** `20260713000000` makes `gdpr_erase_subject` fail **closed** (RAISE when 0 rows match, in all three subject branches) so the invariant holds for every caller, not just that path (needs `supabase db push` to apply). +4 regression tests (`tests/privacyErase.test.ts`, verified to fail without the guard: foreign subject → no ban, no RPC, 404; in-org subject bans; contact/lead skip the profile pre-check; admin+org gates intact). `typecheck` + `lint:ci` + `test:run` green (edge TS is typechecked at deploy). Also catalogued (not yet fixed, ranked for a decision): webhook SSRF via redirect-following (HIGH), pipelines/pipeline_members/automation_executions RBAC gap writable by viewer (MEDIUM), public-booking raw-notes cap bypass (MEDIUM). Archivos modificados: supabase/functions/_shared/privacy.ts, supabase/migrations/20260713000000_gdpr_erase_fail_closed.sql, tests/privacyErase.test.ts, CHANGELOG.md.
- [2026-07-13 12:50 UTC] Frontend/Backend: **Booking reminders, organizer timezone + fixed the garbled email subject.** Two fixes to the booking confirmation/reminder emails the user reported. (1) **Timezone:** `booking_pages.timezone` (backend + API already supported it) had no UI, so it stayed at the schema default `'UTC'` and emails read e.g. "1:30 PM (UTC)" instead of the owner's local time. Added a **timezone selector** to the booking-page editor (full IANA list via `Intl.supportedValuesOf`, curated fallback), defaulting to the creator's browser zone, and new pages now seed that zone instead of UTC, slots + confirmation/reminder emails adapt. i18n `booking.timezone` across all 6 locales. (2) **Subject mojibake:** the hand-rolled Gmail MIME builders sent the `Subject` header as raw UTF-8 with no RFC 2047 encoding, so a non-ASCII char (the U+202F narrow-no-break-space ICU72+ emits before AM/PM, or an em-dash) rendered as `Ã¢Â€Â¯`. Added an `encodeHeaderWord` (RFC 2047 `=?UTF-8?B?…?=`, ASCII passes through) at both builders (`email-runner/index.ts`, `services/gmailService.ts` ×2, fixes booking + sequence + interactive Gmail subjects with unicode names/emoji), switched the UTF-8 body parts from `7bit`→`8bit`, and normalized U+202F/U+00A0 in the formatted time as defense-in-depth (SMTP/Resend + bodies). +4 regression tests (subject encoded-word round-trip + ASCII passthrough; timezone selector reflects/saves + new-page seed), verified to fail without the fix. `typecheck` + `lint:ci` + `test:run` + `build` green. Archivos modificados: supabase/functions/{email-runner,public-booking}/index.ts, services/gmailService.ts, views/BookingPages.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/services/gmailService.test.ts, tests/views/BookingPages.test.tsx, CHANGELOG.md.
- [2026-07-13 12:57 UTC] Backend: **Booking emails now carry a real .ics calendar invite.** The confirmation + reminder previously only offered a Google-only "add to calendar" web link; now they attach a proper RFC 5545 `invite.ics` so the meeting drops into ANY calendar (Google/Apple/Outlook) in one click. New shared `_shared/ics.ts` (`buildIcs`/`icsAttachment`) builds a single-VEVENT VCALENDAR, `METHOD:PUBLISH`, UTC `DTSTART`/`DTEND`, TEXT escaping (`;`, `,`, `\`, newlines), 75-octet line folding, ORGANIZER (best-effort host email lookup) + ATTENDEE, and both scheduled emails attach it with the **same UID** (`booking-<cancelToken>@propeltech.es`) so confirmation + reminder update one event instead of duplicating. The email-runner/SMTP/Resend paths already relay `payload.attachments`, so no pipeline change was needed. 7 unit tests (well-formedness, UTC formatting, escaping, conditional ORGANIZER/ATTENDEE, line-folding, base64 round-trip incl. unicode). `typecheck` + `lint:ci` green; edge TS typechecked at deploy. Archivos modificados: supabase/functions/_shared/ics.ts, supabase/functions/public-booking/index.ts, tests/ics.test.ts, CHANGELOG.md.
- [2026-07-13 13:26 UTC] Backend/Frontend: **Editable, variable-driven booking email templates.** A booking page can now override the confirmation/reminder **subject + body** with its own copy using `{{tokens}}` resolved per-invitee at send time, the same merge-field idea the campaigns use, brought to meetings. New shared `_shared/bookingTemplates.ts` (`renderBookingTemplate` + defaults + the token set: `firstName`, `name`, `title`, `when`, `timezone`, `duration`, `manageLink`, `calendarLink`); unknown tokens resolve to '' (never leaks a raw `{{token}}`) and a value that looks like a token isn't re-expanded. `public-booking` renders the page's templates (or the built-in defaults when a field is blank, so existing pages are unchanged) against the invitee's booking. Storage: 4 nullable columns on `booking_pages` (migration `20260713010000`, idempotent; needs `db push`); exposed + allowlisted in the `/booking-pages` CRUD handler; typed in `bookingService`. Editor UI: an "Email templates" section (subject inputs + body textareas + a variables hint) in the booking-page editor, i18n across all 6 locales. +6 unit tests (token substitution, whitespace tolerance, unknown→'', no recursive expansion, defaults resolve clean) + editor round-trip test. `typecheck` + `lint:ci` + `build` green. Archivos modificados: supabase/functions/_shared/{bookingTemplates.ts,integrations.ts}, supabase/functions/public-booking/index.ts, supabase/migrations/20260713010000_booking_email_templates.sql, services/bookingService.ts, views/BookingPages.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/bookingTemplates.test.ts, tests/views/BookingPages.test.tsx, CHANGELOG.md.
- [2026-07-13 14:02 UTC] Frontend: **Public booking page shows times in the page's timezone (not hardcoded UTC).** After the organizer-timezone fix, the confirmation/reminder emails render in the page tz but the public `/book/<token>` page still showed slots in UTC, a confusing label mismatch. `PublicBooking` now formats slot/day/confirmation times with `config.timezone` (falls back to UTC on a missing/invalid zone) and surfaces the zone ("times shown in Europe/Madrid" + a `(Madrid)` tag), so the visitor sees the same moment the email states. Frontend-only (deploys via Vercel). +1 regression test (09:00 UTC slot renders as 11:00 in Europe/Madrid, and the zone label shows). `typecheck` + `lint:ci` + `build` green. Archivos modificados: views/PublicBooking.tsx, tests/views/PublicBooking.test.tsx, CHANGELOG.md.
- [2026-07-14 08:18 UTC] Frontend/Design: **Inbox redesign, Gmail-caliber, in Propel's identity (light only).** Reworked the three inbox panels after an approved mockup, every control kept wired to its real handler (no dead buttons). **List rows** (`features/inbox/components/InboxThreadItem.tsx`, new component the panel maps over): colour-tinted sender monogram (checkbox overlays on hover/bulk-select), bold-only unread + subtle tint (dropped the redundant dot), grey date, subtle star (`star`/`unstar`), attachment clip only when a message really has one, a CRM tag only when the sender matches a real contact, fixed `min-h` so selection never resizes a row, and floating hover quick-actions (archive · snooze · read/unread · trash) wired to the existing `runThreadAction`. **Reading pane** (`ThreadView`): one tidy action bar, Star/Flag toggles · Mark read/unread · Archive · Trash · Label, with snooze/spam/print folded into a **More** overflow; the misaligned Contact/Deal/Save-link editor rebuilt as a matched-height, labelled row; and the AI summarize/draft results moved out of the cramped toolbar into **full-width cards** (`AiInsight` gained an opt-in `fluid` prop; its other app-wide uses are unchanged). **Left rail** (`FolderNav`): restyled (green pill Compose, accent account chip, clear active state), decorative-only items (CRM-label nav, Starred/Important, storage bar) deliberately omitted for lack of backing data. i18n: added `inbox.reportSpam`/`inbox.print` across 6 locales. Tests: new `InboxThreadItem` suite (8) + extended `ThreadView` (More menu, mark-unread, aligned Save, AI actions), updated `ThreadListPanel`. `typecheck` + `lint:ci` + **2543 tests** + `build` green. PRs #344, #345, merged. Archivos modificados: features/inbox/components/InboxThreadItem.tsx, views/inbox/{ThreadView,ThreadListPanel,FolderNav,InboxPage}.tsx, components/ai/AiInsight.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/views/inbox/{ThreadView,ThreadListPanel}.test.tsx, tests/features/inbox/InboxThreadItem.test.tsx, CHANGELOG.md.
- [2026-07-14 10:40 UTC] Backend/Frontend/Cybersec: **Email marketing compliance, new subsystem.** A full opt-in/opt-out layer so marketing sends are CAN-SPAM/RFC-8058-clean. New `_shared/emailCompliance.ts`: HMAC-signed, self-verifying **one-click unsubscribe token** (carries `{orgId,email}`, no storage), `List-Unsubscribe` + `List-Unsubscribe-Post` headers, a sender-identity/opt-out **footer**, and the **`email_suppressions`** table (org-scoped opt-out/bounce/complaint list, `20260714000000`; RLS read-only to members, all writes via service-role edge paths). The **`email-runner`** now consults the suppression list before every marketing send, bulk `payload.marketing` rows **and all sequence steps**, stamps the footer + headers, and enforces a per-org **daily cap** (`MARKETING_DAILY_CAP`, default 500; over-cap rows deferred, not dropped). Two new public **`email-track`** routes: **`/unsubscribe`** (GET confirm page + POST one-click; neutral non-leaking 200s, never 500s) and **`/resend-webhook`** (Resend bounce/complaint → suppression; Svix-verified via `RESEND_WEBHOOK_SECRET`, **accept-and-log until that secret is provisioned**). Consent capture is **opt-in only, never defaulted on**, added to ContactForm, CSV import, public web forms, and the bulk-send audience filter; new `leads.marketing_opt_in{,_at,_source}` columns (`20260714010000`) mirror the pre-existing `contacts` ones. PR #346, merged. Archivos modificados: supabase/functions/_shared/{emailCompliance.ts,emailOutbound.ts}, supabase/functions/{email-runner,email-track,public-forms}/index.ts, features/inbox/enqueueBulkSends.ts, components/{contacts/ContactForm,import/CSVImport}.tsx, lib/csvRowSchemas.ts, supabase/migrations/{20260714000000_email_suppressions,20260714010000_leads_marketing_optin}.sql, tests/emailCompliance.test.ts.
- [2026-07-14 11:05 UTC] Frontend: **Hid vertical scrollbars app-wide (vertical axis only).** Scroll still works; the visible track is suppressed on the vertical axis, horizontal scrollbars (tables, wide diagrams) are untouched. PR #347, merged.
- [2026-07-14 11:40 UTC] Backend/Frontend: **Campaigns / Newsletter, new feature.** A one-shot marketing broadcast to a filtered, marketing-opted-in contact audience. `views/Campaigns.tsx` + `store/campaignsStore.ts` reuse `enqueueBulkEmailJobs(…, { marketingOnly: true })`, so a campaign send flows through the same compliance pipeline (suppression check + unsubscribe footer/headers + daily cap) as any marketing blast. New org-scoped **`campaigns`** table with tier-1 RBAC (writes gated by `jwt_can_write()`, `20260714020000`); the live "eligible recipients" count mirrors the send-path gate (real email AND `marketingOptIn`). Route `/campaigns` (gated `email:read`; compose/send gated `email:send`) + a sidebar nav item. PR #348, merged. Archivos modificados: views/Campaigns.tsx, store/campaignsStore.ts, components/{ClientApp,layout/Sidebar}.tsx, types/index.ts, supabase/migrations/20260714020000_campaigns.sql, i18n/{types,en,es,pt,de,fr,it}.ts, tests/views/Campaigns.test.tsx.
- [2026-07-14 12:10 UTC] DevOps/Backend: **Edge functions deployed from the cloud session via the Supabase MCP.** Used the MCP `deploy_edge_function` tool (bundle entrypoint + full transitive `_shared` closure, each function's `verify_jwt` preserved) to ship **email-track v12, email-runner v59 (`verify_jwt=true`), public-forms v75**, and MCP `execute_sql` to apply the `email_suppressions`/`leads`/`campaigns` migrations, resolving the pending-deploy runbook (`docs/reviews/2026-07-09-pending-edge-deploy.md`), which had recorded cloud-session deploy as not viable. Follow-up ops task: set `RESEND_WEBHOOK_SECRET` (the Resend webhook runs accept-and-log until then).
- [2026-07-14 13:30 UTC] Document: **Refreshed the living docs for this session's features.** Documented the email-marketing-compliance subsystem + Campaigns in CLAUDE.md (§4 locked `email_suppressions`, §5 function table + shared modules, §6 subsystems, §8 the MCP `deploy_edge_function` path + `RESEND_WEBHOOK_SECRET`), added the #344–#348 entries here, added the v2.8.0 release notes to docs/RELEASES.md, marked the edge-deploy review RESOLVED, and added Campaigns + compliance to the README. Docs-only, no source touched. Archivos modificados: CLAUDE.md, CHANGELOG.md, README.md, docs/RELEASES.md, docs/reviews/2026-07-09-pending-edge-deploy.md.
- [2026-07-14 14:05 UTC] Frontend/Design: **Landing page, full redesign.** Rebuilt `PropelLanding.tsx` (870 → 404 lines) around extracted subcomponents in `components/marketing/landing/`, a new hero + `HeroVisual`, a **bento** feature grid (`Features.tsx`), a **dark security set piece** (`Security.tsx`), a `Pricing.tsx` section, and a shared `ui.tsx` primitive set. **Zero new dependencies**; all motion is gated behind `prefers-reduced-motion` (`useReveal.ts`), and the section styles landed in `app/globals.css`. PR #350, merged. Archivos modificados: components/marketing/PropelLanding.tsx, components/marketing/landing/{Features,HeroVisual,Pricing,Security,ui}.tsx, components/marketing/landing/useReveal.ts, app/globals.css, tests/components/marketing/PropelLanding.test.tsx.
- [2026-07-14 14:25 UTC] Frontend: **User profile, redesign.** Reworked `views/UserProfile.tsx` into a compact identity header with editable **job title / phone** that persist, a verified avatar upload, and a **real last sign-in**, `lib/supabase/auth.ts` now carries `last_sign_in_at` off the Supabase auth user into `lastLoginAt` (login + restore-session paths), replacing the old "N/A". Rebranded the **role badges to the green/mint palette** in `utils/permissions.ts` (`ROLE_COLORS`), the admin pill is no longer danger-red, fixing it everywhere the badge renders app-wide. PR #351, merged. Archivos modificados: views/UserProfile.tsx, lib/supabase/auth.ts, utils/permissions.ts, components/layout/Topbar.tsx, tests/views/UserProfile.test.tsx.
- [2026-07-14 14:50 UTC] Frontend: **Marketing site i18n, full localization, new `i18n/marketing/` module.** Localized the redesigned landing **and all 11 marketing subpages** (about/blog/careers/contact + the 7 legal pages) into the app's 6 locales (en/es/pt/fr/de/it). Copy lives in a **standalone module**, `i18n/marketing/` (`types.ts`, `useMarketingCopy.ts`, per-locale dicts) with **tsc-enforced 6-locale parity**, SEPARATE from the app `i18n/*.ts` dictionaries. `i18n/marketing/legal/` holds **courtesy translations** of the 7 legal pages, each rendering a **prevailing-language notice** when the viewing locale differs from the authoritative one (English authoritative for all except `/aviso-legal` → Spanish/LSSI-CE), via `components/marketing/legal/LegalPage.tsx`. New `LocaleSwitcher` (nav + footer) writes the SAME persisted `useI18nStore` preference the app uses, so a language chosen on the landing carries into login/app and back; `useMarketingCopy()` mirrors the locale onto `document.documentElement.lang`. Static pages now render from the shared copy (`components/marketing/pages/{About,Blog,Careers}.tsx`). Login/Register were already localized via the app dictionary. PR #352, merged. Archivos modificados: i18n/marketing/** (types, useMarketingCopy, {en,es,pt,fr,de,it}.ts, legal/**), components/marketing/{LocaleSwitcher,chrome,CookieConsent,PropelLanding}.tsx, components/marketing/legal/LegalPage.tsx, components/marketing/landing/{Features,HeroVisual,Pricing,Security}.tsx, components/marketing/pages/{About,Blog,Careers}.tsx, app/{about,blog,careers,contact,privacy,terms,gdpr,cookies,acceptable-use,aviso-legal,subprocessors}/page.tsx.
- [2026-07-14 15:05 UTC] Backend/Ops: **Plans cleanup, 3 legacy plans deleted from prod.** Removed the three stale legacy plans from the production `plans` data so the catalog is now exactly **4 active plans** (monthly): **Starter €39 · Growth €79 · Business €149 · Enterprise €299**. Still **no free tier**. Prod-data change (no repo diff).
- [2026-07-14 15:20 UTC] DevOps: **Branch model change, `master` retired for `production` + `stagging`.** The repo now runs two long-lived branches. **`production`** is the GitHub default branch (HEAD) and the **only** branch Vercel deploys to the live site (propeltech.es); **`stagging`** (sic, spelled that way in the repo) is the integration branch. Going forward: feature branches → PR **into `stagging`** → promote `stagging`→`production` (fast-forward) **only on the user's explicit OK**. Both branches are currently in sync. This is a **git + Vercel** flow only, there is still one prod Supabase behind all branches (no database staging), so "treat every change as prod" still holds. Repo/Vercel config change (no source diff).
- [2026-07-14 16:00 UTC] Document: **Refreshed the living docs for #350–#352 + the branch-model change.** Documented the landing redesign, profile redesign, and the standalone `i18n/marketing/` module in CLAUDE.md (header branch line, golden rules #3/#6, §6 subsystem + 4-plan billing note, §8 branch/deploy flow, §9 feature-branch flow); added the #350/#351/#352, plans-cleanup, and branch-model entries here; added the v2.9.0 release notes to docs/RELEASES.md; and updated README (localized marketing site + redesigned landing, `master`→`production`/`stagging` in the Environments/CI sections). BACKLOG/MASTER_PLAN untouched (they track none of this as pending). Docs-only, no source touched. Archivos modificados: CLAUDE.md, CHANGELOG.md, README.md, docs/RELEASES.md.
- [2026-07-14 21:10 UTC] Frontend/SEO: **Branded error surfaces + marketing-site SEO (#354–#356).** Added `app/not-found.tsx` (branded 404, noindex), `app/error.tsx` (route-segment boundary → Sentry + reset) and `app/global-error.tsx` (own html/body for root failures), all in the Propel palette with self-contained styles. Then the SEO layer: `app/sitemap.ts` + `app/robots.ts`, `metadataBase` + title template + default OG/Twitter cards on the layout, per-page canonical/OG metadata across the 11 marketing pages, site-wide JSON-LD (Organization, SoftwareApplication with per-plan Offers, FAQPage), and a branded 1200×630 `public/og.png` share card (ink backdrop, chevron mark, "Propel every deal forward."). hreflang omitted by design (client-SPA, one URL per page). PRs #354, #355, #356, merged. Archivos modificados: app/{not-found,error,global-error}.tsx, app/{sitemap,robots}.ts, app/*/page.tsx, lib/seo.ts, public/og.png.
- [2026-07-14 22:00 UTC] Backend/Frontend: **Campaigns v2 + Sentry actually booted (#357, #358).** Campaigns grew up while reusing the compliance send path (`enqueueBulkEmailJobs marketingOnly:true`, no new send surface): `renderCampaignEmail()` wraps the composer body (via the XSS-safe `formatPlainToHtml`) in a branded, responsive, table-based HTML shell + live iframe preview; **scheduling** (`campaigns.scheduled_at` + a `'scheduled'` status, sendAt → `scheduled_emails.run_at`, cancel flips pending rows to `'canceled'` and reverts to draft); **per-campaign metrics** (rows tagged `payload.campaignId`, cards aggregate recipients/sent/failed/skipped); open/click analytics scaffolded only (`email_tracking_messages.campaign_id`, not populated). CampaignComposer/CampaignCard extracted to keep files <500 lines; migration `20260714030000` (idempotent). And Sentry was wired but dead, `initSentry()` was never called and the DSN was read through a non-inlined dynamic helper: now `lib/sentry.ts` reads the literal `process.env.NEXT_PUBLIC_SENTRY_DSN`, a new `components/SentryBoot.tsx` boots it after first paint behind a literal-DSN guard + dynamic import (the ~185KB chunk loads only when configured), mounted once in the root layout; `deploy.yml` syncs `RESEND_API_KEY/RESEND_FROM/RESEND_WEBHOOK_SECRET` to the edge and `vercel-env-sync.yml` upserts the DSN. PRs #357, #358, merged. Archivos modificados: views/Campaigns.tsx, components/campaigns/{CampaignComposer,CampaignCard}.tsx, store/campaignsStore.ts, utils/campaignEmail.ts, supabase/migrations/20260714030000_campaigns_v2.sql, lib/sentry.ts, components/SentryBoot.tsx, app/layout.tsx, .github/workflows/{deploy,vercel-env-sync}.yml, .env.example, i18n/*.
- [2026-07-14 23:30 UTC] Frontend: **Sales Intelligence on the Dashboard (#359–#361).** The home screen now tells the rep what to do next, fully client-side from data already in the stores (no migration, no edge, no new deps): a pure `features/intelligence` engine ranks a de-duplicated **next-best-actions** list across deals (overdue/closing-soon/stalled), overdue tasks and leads (uncontacted/hot), plus a **Deals at risk** roll-up, surfaced as two panels above the revenue row with urgency dots, health badges and deep links; new `intelligence` i18n namespace across all 6 locales. A fast-follow (#360) reconciled its bespoke deal-health model with the shared `utils/dealHealth.computeDealHealth` so the dashboard, deals list, kanban and Forecast all agree on one health per deal. DB advisor hardening (#361, applied to prod via MCP): covering index on `campaigns.created_by` + `SET search_path=''` on two trigger functions; the deliberately-not-changed advisor findings are documented in the migration. PRs #359, #360, #361, merged. Archivos modificados: features/intelligence/index.ts, components/dashboard/SalesIntelligence.tsx, views/Dashboard.tsx, utils/dealHealth.ts, i18n/*, supabase/migrations/20260715000000_advisor_hardening.sql, tests/*.
- [2026-07-15 09:30 UTC] Observability: **Sentry envelopes tunneled same-origin (`/monitoring`).** Ad/tracker blockers kill the `*.ingest.*.sentry.io` hosts (`net::ERR_BLOCKED_BY_CLIENT`, confirmed live in prod), silently dropping every blocker-running user's errors. `Sentry.init` now uses `tunnel: '/monitoring'` and the new route handler `app/monitoring/route.ts` forwards envelopes after validating each envelope's DSN against our own project (never an open relay; size-capped body; upstream failures never surface). Verified end-to-end against prod Sentry. 7 route tests + the init test asserts the tunnel. Direct commit to `stagging` (6f278a8). Archivos modificados: lib/sentry.ts, app/monitoring/route.ts, tests/lib/sentryTunnel.test.ts, README.md, CLAUDE.md.
- [2026-07-15 10:15 UTC] QA/Backend/Frontend: **Morning audit wave (#362–#366).** (#362) Grounded read-only audit fixes: deal "days in stage" + Dashboard revenue/velocity now key off `stageChangedAt` (a note edit no longer refreshes a stalled deal or moves revenue between months); Forecast "Best bets" converts to base currency before ranking; a deal with no expected close date is no longer flagged overdue; contacts/activities `dateTo` filters compare date-only (records created on the final day were dropped); the Contacts grid card is keyboard-activatable (WCAG 2.1.1). (#363) `api_keys.user_id` (nullable FK → profiles, ON DELETE CASCADE, applied to prod): foundation for per-user tokens + MCP: when set, the key acts as that user; offboarding cascades to revoke their tokens; the auth surface itself is deliberately deferred. (#364) `followUpEngine.suggestAction` returned hardcoded Spanish; it now returns a stable kind resolved through a new `followUps.actions` namespace (9 keys × 6 locales). (#365) Em-dashes removed across the codebase (498 files, punctuation normalization). (#366) Inbox Gmail-parity: the folder rail shows unread OR total (never both) and opening a thread auto-marks it read (delayed, once per open; the redundant "Mark read" button dropped). PRs #362–#366, merged. Archivos modificados: utils/dealHealth.ts, utils/followUpEngine.ts, views/{Dashboard,Forecast,Contacts,FollowUps}.tsx, components/deals/DealCard.tsx, store/{contacts,activities}Store.ts, views/inbox/*, i18n/*, supabase (api_keys.user_id migration via MCP).
- [2026-07-15 11:00 UTC] QA/Frontend: **Bug-hunt wave 1 + honest admin status (7+2 fixes).** Seven correctness bugs from a parallel source audit, each with a concrete failing input: Activities calendar bucketed by UTC date while cells key on local (an evening activity showed on the wrong day off-UTC); Forecast's engagement ratio counted closed-deal activities in the numerator only (gauge could read >1); the quote EMAIL omitted the withholding (IRPF) line its own total subtracts; `parseCSV` tore apart quoted fields spanning lines (multi-line notes in HubSpot/Salesforce exports mis-aligned every following row; rewritten as a single-pass tokenizer); `parseContactSource` didn't normalize case ("LinkedIn" collapsed to `other`); the Topbar overdue filter flagged activities due TODAY (full-ISO vs date-only compare); Dashboard "Top deals" ranked raw mixed-currency values (now base-converted). Plus (99ec028): the Admin org-status badge derived purely from the subscription and ignored `organizations.status`, so a no-sub org always read "suspended" and reactivation never changed it; `adminOrgStatus()` now takes org status first and a no-plan-but-not-suspended org reads a distinct `inactive`. ChoosePlan cards got their missing white `bg-surface-1`. Direct commits 4271cca, 99ec028 to `stagging`. Archivos modificados: views/{Activities,Forecast}.tsx, views/deals/QuoteBuilder.tsx, lib/{csvParse,csvRowSchemas}.ts, components/layout/Topbar.tsx, components/dashboard/CustomDashboard.tsx, supabase/functions/_shared/handlers/admin.ts, views/{Admin,ChoosePlan}.tsx, tests/*.
- [2026-07-15 11:30 UTC] Backend/Frontend: **Trial lifecycle: trials that actually end, and signups that actually start (bdbb20d + cae4015).** Before: entitlements granted access for any `trialing` subscription forever (nothing checked `trial_ends_at` without Stripe), and a brand-new org had NO subscription at all, stuck at /choose-plan with checkout impossible until Stripe is configured. Now: (1) `expire_trials()` + hourly pg_cron `propel-expire-trials` flips `trialing→expired` once `trial_ends_at` passes (migration `20260715100000`, applied to prod); (2) `create_organization()` grants a **14-day `trialing` Growth subscription at org birth** (in its own BEGIN/EXCEPTION so onboarding can never break; `20260715110000`, applied), so self-service signup now works WITHOUT Stripe, which only converts the trial to paid; (3) `entitlements.ts` also enforces expiry at read time, so access ends the instant the trial lapses, not on the next cron tick. Plus a visual-only **ChoosePlan redesign** (cae4015): brand hero, pill interval toggle, elevated "Popular" Growth card with accent ring, display-font prices, green-check feature lists, tinted compare table; same hooks/checkout/logic. Archivos modificados: supabase/migrations/{20260715100000_expire_trials,20260715110000_signup_trial}.sql, supabase/functions/_shared/entitlements.ts, views/ChoosePlan.tsx.
- [2026-07-15 12:15 UTC] QA/Frontend/Backend: **Bug-hunt wave 2: admin lists, Gmail links, calendar TZ, filters (89dd23a).** `/admin/orgs` & `/admin/users` honored `page/pageSize` while the client sends `limit/offset`, so "Next" always returned page one and the CSV export capped at 25 rows, and server-side search + plan filter were ignored entirely (now: limit/offset honored, `ilike` name/email(+slug) search, inner-join plan filter, plus a hard `DELETE /admin/users/:id` with self- and last-super-admin guards + confirm-dialog UI). `GET /gmail/thread-links` returned snake_case rows the store reads as camelCase, so every thread→record link keyed `undefined` and associations vanished on reload. Date-only deal/activity dates parsed as UTC midnight placed events a day early west of UTC (`parseCalendarDate` anchors local). A `dueDateTo` deals filter kept no-close-date deals; a phone-dup group was hidden when only one member wasn't email-grouped; deal CSV import no longer defaults currency to USD (leaves it empty for base-currency fill); email-tracking stats gained the org filter its siblings carry. Regression tests for each; suite 2676 green + build. Archivos modificados: supabase/functions/_shared/handlers/{admin,gmail}.ts, views/Admin.tsx, utils/calendar.ts, views/deals/DealFilters.tsx, views/Duplicates.tsx, lib/crmMigrationImport.ts, i18n/*, tests/*.
- [2026-07-15 12:30 UTC] Cybersec/Backend: **Impersonation is now WRITE-ENABLED + fully audited (21520e1 + c12a993, product decision, reverses the 07-10/07-11 read-only stance).** A support session exists so an operator CAN fix a customer's data. RLS (`20260715120000`): `jwt_can_write`/`jwt_is_admin`/`jwt_has_permission` no longer short-circuit during impersonation, so the operator has admin-level write over the impersonated tenant; `jwt_is_impersonating()` is kept as the audit flag. Every write is attributed by `audit_row_change()` (operator's real `auth.uid()` + target org) and every edge-routed request is logged to the new **`support_access_log`** (`20260715130000`: org, operator, method, path, request-id; org members can read their own log, customer transparency; writes service-role only, append-only). `assertNotImpersonating()` is now a documented no-op kept as the single choke point if read-only ever returns; the ONE thing still blocked mid-session is irreversible destruction: `assertNotImpersonatingIrreversible()` gates GDPR erase (anonymize + permanent ban). Also fixed (c12a993): `getAuthContext` derived `ctx.orgId` from the operator's own profile row while RLS/PostgREST resolved the TARGET org, so edge-backed screens showed the operator's tenant mixed with the target's; `ctx.orgId` now follows the `impersonating_org` claim, and `POST /privacy/erase` gained the impersonation guard it lacked (403, no ban, no RPC, regression-tested). Archivos modificados: supabase/migrations/{20260715120000_impersonation_write_enabled,20260715130000_support_access_log}.sql, supabase/functions/_shared/{auth,privacy}.ts, supabase/functions/propel-api/index.ts, tests/*.
- [2026-07-15 13:40 UTC] Frontend/Backend: **/docs, a native, login-gated developer documentation portal (dadbbc6 + a00010e).** A branded docs portal inside the authenticated app (no anonymous access): `views/DocsPortal.tsx` + route `/docs` in the AppShell. Access is per account: super-admins always in; everyone else sees a polished "Request access" screen until enabled. Backend grant model: `profiles.docs_access` + `docs_access_requested_at` (migration `20260715140000`; privilege columns, never client-writable), `profileToAuthUser` maps → `currentUser.docsAccess`, `POST /docs/request-access` stamps the request, and Admin → Users gained a per-row **Docs** grant toggle (granted / requested / none) over `PATCH /admin/users/:id { docsAccess }` (granting clears the marker). i18n `docs` chrome namespace (en/es/pt full; de/fr/it inherit) + admin keys across all 6. Tests: gate vs. granted, admin toggle, mapper. Archivos modificados: views/DocsPortal.tsx, components/ClientApp.tsx, supabase/migrations/20260715140000_docs_access.sql, lib/supabase/auth.ts, types/auth.ts, supabase/functions/_shared/handlers/admin.ts, supabase/functions/propel-api/index.ts, views/Admin.tsx, i18n/*, tests/*.
- [2026-07-15 19:30 UTC] Frontend/Document: **/docs portal v2 (complete API reference) + full .md catch-up.** The portal grew from 4 thin placeholder sections to the complete, code-verified reference: 10 sections in a grouped sidebar (Getting started, Authentication, Resources & verbs, Fields per resource, Pagination, Filtering & sorting, Scopes, Rate & plan limits, Errors, Webhooks), a mobile section picker, copy-to-clipboard code blocks, a link to the live `openapi.json`, and the real project base URL baked into every example (`getSupabaseUrl()`). Content now matches the code where v1 (and in two cases `docs/api-public.md`) had it wrong: there is **no `/v1` path segment** (`/public-api/contacts`, not `/public-api/v1/contacts`), keys are **`pk_<hex>`** (not `pk_live_`), scopes are **fail-closed** (empty grants nothing; creation defaults to `read`; the "empty = full access" note was stale), and the webhook signature is **`X-Propel-Signature: sha256=<hex HMAC>`** + `X-Propel-Event` with a `{ event, data, timestamp }` body (v1 showed an invented Stripe-style `t=…,v1=…`), with the real event set (`lead.created`, `meeting.ended`, `test`) and an HMAC verification snippet. Split into `views/docs/{primitives,apiReference,apiGuides}.tsx` to keep every file <500 lines; a "Documentation" link (reusing `t.docs.title`, no new i18n keys) added to Settings → API & capture. Docs refresh in the same push: CHANGELOG caught up on #354–#366 + the whole 07-15 session (11 new entries), README (impersonation model flipped to write-enabled+audited in its 3 claims, new features section refreshed, docs portal + Sales Intelligence + trial lifecycle added), docs/RELEASES.md v2.10.0, CLAUDE.md (§5 gains the missing `automation-runner` row, 8→9 functions, both runners' `verify_jwt=true` documented; §6 /docs + Sales Intelligence subsystems, billing trial lifecycle, Campaigns v2), docs/api-public.md corrections (key prefix, fail-closed scopes, portal cross-link), docs/MASTER_PLAN.md (read-only-impersonation note annotated with the 07-15 reversal; test counts), docs/BACKLOG.md (the stale `#170` deploy item rewritten to the real pending state: propel-api redeploy + 07-15 migrations), docs/TEAM_PROCESS.md + docs/disaster-recovery.md (`master` → `production`/`stagging` branch model), and test counts everywhere to the fresh run (**2,682 tests / 305 files**). Also un-broke `npm run check:branding`, red since #329 because the demo-seed's default owner account is a real pre-rebrand email; `seed-demo.mjs` is now skip-listed with a comment (account data, not product branding), the failure was invisible in CI because Actions runners fail in ~2s on account minutes. Tests 5 for the portal (nav, content, OpenAPI link) + the Settings panel test wrapped in a router. Full gate: typecheck 0 · lint:ci OK · branding OK · **2682 tests green** · build 0. Archivos modificados: views/DocsPortal.tsx, views/docs/{primitives,apiReference,apiGuides}.tsx, components/settings/SettingsIntegrationsPanel.tsx, tests/views/DocsPortal.test.tsx, tests/components/settings/SettingsIntegrationsPanel.test.tsx, scripts/check-branding.mjs, CHANGELOG.md, README.md, CLAUDE.md, docs/RELEASES.md, docs/api-public.md, docs/MASTER_PLAN.md, docs/BACKLOG.md, docs/TEAM_PROCESS.md, docs/disaster-recovery.md.
- [2026-07-16 08:30 UTC] Frontend/Observability: **PostHog product analytics + Web Vitals, consent-gated.** Integrated PostHog by hand instead of running the interactive `@posthog/wizard` (its default init captures before consent, wrong for an LSSI-CE/GDPR EU product, and the wizard is interactive). Mirrors the Sentry boot pattern: `lib/posthog.ts` (`initPostHog`/`disablePostHog`, EU host `NEXT_PUBLIC_POSTHOG_HOST` default `eu.i.posthog.com`, `person_profiles: 'identified_only'`, Web Vitals via `capture_performance.web_vitals`) + `components/PostHogBoot.tsx` mounted in the root layout. **Consent-gated by design:** posthog-js is dynamic-imported and initialised ONLY when `NEXT_PUBLIC_POSTHOG_KEY` is set AND the visitor accepted analytics in `CookieConsent` (`propel_cookie_consent === 'all'`), so the 72.6KB posthog-js chunk is never downloaded for an essential-only choice, and accepting live-boots it with no reload. Verified: posthog-js lands in its own lazy chunk (72.6KB gzip), the critical chunk is unchanged (173.4KB), CI's prod `npm audit` stays clean (no new critical). 6 consent-gating tests (no key / no consent / essential-only → no init; key+`all` → EU-host init; idempotent; custom host). Ships dark until `NEXT_PUBLIC_POSTHOG_KEY` is set in Vercel env. Known gap (documented): the consent banner only renders on the marketing site, so in-app-only visitors stay un-tracked until an in-app consent control exists. `typecheck` 0 · `lint:ci` clean · `build` 0. Archivos modificados: lib/posthog.ts, components/PostHogBoot.tsx, components/marketing/CookieConsent.tsx, app/layout.tsx, .env.example, package.json, tests/lib/posthog.test.ts, README.md, CLAUDE.md.
- [2026-07-16 09:45 UTC] Frontend/Design: **Inbox selection checkbox restyle (was visually broken).** The bulk-select control in `InboxThreadItem` was a native `<input type=checkbox>` stretched to 36×36px and forced `rounded-full`, so the browser drew a small square native check inside a large round border (crude and inconsistent across browsers, especially a whole column of them in bulk-select mode). Replaced with a self-styled 20px `rounded-md` checkbox centred in the avatar slot: subtle border when empty, brand-green fill + white `Check` when selected. The real `<input>` stays (sr-only, accessible, same `aria-label`), so only the visual changes; monogram in the resting state unchanged. 8 InboxThreadItem tests green · typecheck 0 · lint clean · build 0. Archivos modificados: features/inbox/components/InboxThreadItem.tsx, CHANGELOG.md.
- [2026-07-16 10:05 UTC] QA/Analytics: **Validated + fixed the PostHog wizard's PR #375 before promoting it.** The PostHog EU bot opened & merged its own "Add PostHog analytics integration" (#375) onto `stagging`, on top of the hand-rolled consent-gated base: reverse-proxy `/ingest` (next.config rewrites, dodges ad-blockers), `identify` of signed-in users, 12 consent-gated business events (`user_logged_in`/`user_signed_up`/`campaign_sent`/`checkout_started`/`lead_created`/…) via `safeCapture`/`safeIdentify` (all guarded on the `initialized` flag → still opt-in only), `capture_exceptions`, a `posthog-node` server client, and `posthog-node@5.44.0` (official). Security/compliance review: consent gate intact, server client is dead code (never called → no un-consented server capture), dep provenance verified. But it had **never passed CI** (Actions minutes) and broke **2 test files** once run: `tests/lib/posthog.test.ts` asserted the old `api_host` (now `/ingest`, host moved to `ui_host`), and `tests/apiContractEndpoints.test.ts`'s `@/lib/env` mock lacked `getAppChannel` (now pulled in via authStore → posthog → envChannel). Fixed both; full suite back to **2688 green** · typecheck · lint · build · prod-audit clean. Archivos modificados: tests/lib/posthog.test.ts, tests/apiContractEndpoints.test.ts, CHANGELOG.md (+ #375's lib/posthog.ts, components/PostHogBoot.tsx, lib/posthog-server.ts, next.config.ts, store/authStore.ts, views/{Campaigns,ChoosePlan,Leads,OrgSetup,Sequences,deals/DealsPage}.tsx, package.json).
- [2026-07-16 10:30 UTC] Document: **README full redesign + living-docs refresh.** Reworked README.md from a 537-line changelog-dump into a leaner, more scannable ~300-line product doc: tightened header/tagline, a concise **Highlights** section (with the exhaustive history pointed to CHANGELOG/RELEASES instead of inlined), a cleaner **CRM modules** table, a redrawn architecture diagram, a dedicated **Observability & analytics** section (Sentry tunnel + consent-gated PostHog + Vercel Speed Insights), a **Public API & docs portal** section, and an honest **Roadmap** framed by the real maturity levels (SSO/SCIM L0, scale/DR open). Counts refreshed (2688 tests, 76 migrations, 9 edge fns), footer date. Living docs caught up: CLAUDE.md §6 PostHog bullet (reverse-proxy `/ingest` + `ui_host`, consent-gated `safeCapture`/`safeIdentify` business events, `posthog-server.ts` unused, don't re-break the `posthog.test.ts` assertions), MASTER_PLAN (Documentation pillar L2→**L3** with evidence, 8→**9** edge functions, test count 2688/306), docs/RELEASES.md **v2.12.0 "The One Where the App Learned to Watch Itself"**. Historical docs (reviews/adr/legal/specs) left untouched by design (point-in-time records). Docs-only. Archivos modificados: README.md, CLAUDE.md, docs/MASTER_PLAN.md, docs/RELEASES.md, CHANGELOG.md, docs/BACKLOG.md.
- [2026-07-16 11:30 UTC] Frontend: **Fixed the booking-editor day toggles (label overlapping the switch).** In `views/BookingPages.tsx` the per-weekday availability toggle was a hand-rolled `<button>` whose knob `<span>` was `absolute` with **no `left` anchor**, so it fell back to a static position that, plus `translate-x-[18px]`, rendered the knob OUTSIDE the 36px track and on top of the day label (measured: knob 53–69px, track ended at 53px, label started at 65px → "Wed" read "Ved", "Fri" was overlapped). Replaced it with the design-system `<Switch>` (already used twice in the same editor for `enabled`/`createLead`), which anchors the knob with `left-0.5` + `translate-x-5` so it stays in the track. Reproduced + measured + confirmed with a headless-Chromium screenshot (knob now inside the track, 13px clearance to the label). typecheck 0 · lint clean · BookingPages tests 7/7 · build 0. Archivos modificados: views/BookingPages.tsx, CHANGELOG.md.
- [2026-07-16 13:00 UTC] Auth: **Fixed the password-reset page never establishing the recovery session ("Invalid or missing password reset token") even with a valid link.** The `/auth/v1/verify` recovery link delivers the session as an implicit-grant hash (`#access_token&refresh_token&type=recovery`), but our `@supabase/ssr` `createBrowserClient` (PKCE + cookies) does not reliably consume that hash via `detectSessionInUrl`, so `views/ResetPassword.tsx` sat at "missing" while a fully valid token sat in the URL. Now the view consumes the URL credential explicitly — `setSession({access_token, refresh_token})` for the implicit hash, `exchangeCodeForSession(code)` for a PKCE `?code=`, falling back to any session already present — then strips the tokens from the URL/history. Confirmed with the real failing URL (valid, unexpired `type=recovery` token that the client ignored). Separately, the Supabase Auth **Site URL** was pointing at a protected Vercel deployment URL (`crm-project-…-projects.vercel.app`, "You Need Access"); moved to the public domain + allowlisted `/reset-password` (dashboard config, not code). typecheck 0 · lint clean · ResetPassword tests 7/7 (2 new for the Supabase flow). Archivos: views/ResetPassword.tsx, tests/views/ResetPassword.test.tsx, CHANGELOG.md.
- [2026-07-16 14:20 UTC] Auth: **The password-reset email no longer points wherever the user happened to be.** `supabaseForgotPassword` built `redirectTo` from `window.location.origin`, so requesting a reset while browsing a protected Vercel deployment URL (`crm-project-…-projects.vercel.app`) mailed the recovery link straight back to that "You Need Access" gate — and because Vercel's integration keeps re-adding those URLs to Supabase's redirect allowlist, Supabase honored the poisoned origin. Now the link is pinned to `NEXT_PUBLIC_SITE_URL/reset-password` when set, and otherwise omits `redirectTo` so Supabase falls back to its configured Site URL (single source of truth) — never the request origin. Set `NEXT_PUBLIC_SITE_URL=https://propeltech.vercel.app` in Vercel for the explicit path. typecheck 0 · lint clean · supabaseAuth tests 43/43 (2 new) · build 0. Archivos: lib/supabase/auth.ts, tests/lib/supabaseAuth.test.ts, CHANGELOG.md.
- [2026-07-16 19:30 UTC] Security/Impersonation: **Operators can no longer contact an org's third parties during a support session.** Support sessions went write-enabled on 2026-07-15, but that also let an impersonating operator email/enroll/invite the org's external contacts (the composer even showed the operator's own address as sender). Added a new blocked category "contacting third parties" in three layers: **(1) DB backstop** — migration `20260716191630` adds RESTRICTIVE INSERT policies on `scheduled_emails` + `sequence_enrollments` keyed on `jwt_is_impersonating()`, which covers manual compose, bulk email, campaigns, sequence enroll AND the browser-direct Gmail path (all enqueue to `scheduled_emails` via PostgREST + email-runner, with no edge choke point). Applied to prod and verified (impersonating INSERT → 42501 denied; normal INSERT → ok). **(2) Edge** — `assertNotImpersonatingOutbound(ctx)` (sibling of `...Irreversible`) now guards `/email/send`, `/smtp/test`, `/sequences/enrollments`, and `POST/PATCH/DELETE /calendar/` (Google invites send `sendUpdates=all` from the operator's own token); deploys via deploy.yml on promotion. **(3) Client** — `useImpersonation()` disables the composer Send (all entry points), Contacts bulk-email + bulk-enroll, Campaign send/schedule, and the calendar event form; the hook is now defensive (no crash when Supabase env is absent). Config/support writes stay allowed. typecheck 0 · lint 0 · tests 2692/2692 (306 files; +1 composer impersonation test) · build 0. Archivos: supabase/migrations/20260716191630_*.sql, supabase/functions/_shared/{auth,emailOutbound,google}.ts + handlers/sequences.ts, components/email/composer/EmailComposer.tsx, components/calendar/CalendarEventForm.tsx, views/{Contacts,Campaigns}.tsx, hooks/useImpersonation.ts, tests/components/email/EmailComposer.test.tsx, CLAUDE.md, CHANGELOG.md.
- [2026-07-16 20:05 UTC] Frontend: **Fixed the AI "next best action" result cramming the detail-header toolbar (contact + deal).** In `ContactDetail` and `DealsPage` the `AiInsight` button sits in a horizontal `flex` action toolbar, but its result card rendered **in flow** (`inline-flex flex-col`, `max-w-md`) — so when the AI text appeared it expanded that row, shoving the Edit/enroll/delete controls around and looking broken (same class of bug as the composer). Now the non-fluid `AiInsight` result **floats as a popover** anchored under the button (`absolute right-0 top-full z-30`, width-capped, scrollable) so it never disturbs the toolbar; the `fluid` variant (inbox thread view, already correct with its own full-width row) is unchanged. typecheck 0 · lint 0 · tests 2692/2692 · build 0. Archivos: components/ai/AiInsight.tsx, CHANGELOG.md.
- [2026-07-16 20:38 UTC] Impersonation (follow-up): **Closed the last UI outbound gap — the SMTP "send test email" button.** `SettingsSmtpPanel`'s test send (`/smtp/test`) delivers a real message to a real inbox, so it now disables + guards `handleTest` during a support session via `useImpersonation()`, matching the composer/bulk/campaign/calendar guards. (The edge-function guard for `/smtp/test` is coded but pending deploy — see note.) typecheck 0 · lint 0 · settings tests 17/17 · build 0. Archivos: components/settings/SettingsSmtpPanel.tsx, CHANGELOG.md. NOTE: the impersonation EDGE guards (assertNotImpersonatingOutbound in propel-api) are committed + verified-compiling but NOT yet deployed — deploy.yml is failing on GitHub Actions minutes and the 33-file closure (301KB) is too large to deploy via the Supabase MCP in one call. Deploy from a machine with SUPABASE_ACCESS_TOKEN: `supabase functions deploy propel-api --use-api --no-verify-jwt --project-ref fhugkidzcojrnalvqvln`. The RLS backstop (migration 20260716191630) IS live, so compose/bulk/campaigns/enroll/Gmail-direct are already blocked at the DB.
- [2026-07-16 21:20 UTC] Tickets: **You can now paste and drag-drop images into a ticket** (not just the file picker). `views/Tickets.tsx` `TicketDetail` gets an `onPaste` on the panel (acts only when the clipboard carries image files, so pasting text into subject/description is unaffected) and an `onDrop`/`onDragOver` drop zone on the attachment grid (with a ring highlight while dragging); `addPhotos` now accepts `File[]` as well as `FileList` and filters to `image/*`. Reuses the existing `uploadTicketImage` → private `ticket-attachments` bucket + signed-URL render, no schema change. typecheck 0 · lint clean · tickets tests 19/19 · build pending. Archivos: views/Tickets.tsx, CHANGELOG.md.
- [2026-07-16 21:42 UTC] Tickets: **Expanded the ticket tracker into a light project board with a first-class Sprints entity (Model B) and richer fields.** Migration `20260716212656` adds a `sprints` table (name, goal, status planned/active/completed, start/end dates; org-scoped, RBAC via the `tickets:*` permission set, `ON DELETE CASCADE` from org, `ON DELETE SET NULL` back-ref from tickets) plus new `tickets` columns `type` (task/bug/feature/question/incident), `labels text[]`, `due_at`, `estimate`, `sprint_id`. Applied to prod + verified; all CRUD flows direct to PostgREST (`/tickets`, new `/sprints` route in `crmApi`), so **no edge deploy needed**. New `store/sprintsStore.ts` + extended `ticketsStore`. UI (`views/Tickets.tsx` + `components/tickets/SprintsManager.tsx`): the create form and detail drawer now edit type/sprint/due date/estimate/labels (chip input) and surface the linked company; a sprint filter on the toolbar scopes both board and list; a **Manage sprints** drawer does full sprint CRUD with per-sprint ticket counts; cards show a type badge, sprint chip and an overdue indicator. i18n added across all 6 locales + `types.ts`. typecheck 0 · lint clean (2 pre-existing warnings) · tests **2696/2696** (+4 sprintsStore) · build 0. Archivos: supabase/migrations/20260716212656_*.sql, store/{ticketsStore,sprintsStore}.ts, lib/supabase/crmApi.ts, views/Tickets.tsx, components/tickets/SprintsManager.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/stores/{ticketsStore,sprintsStore}.test.ts, tests/views/Tickets.test.tsx, CHANGELOG.md.
- [2026-07-16 22:00 UTC] Design: **App-wide premium refinement at the token/shared-class layer (cascades to every screen, no per-component churn).** In `app/globals.css`: added global font-smoothing (`antialiased` + `optimizeLegibility`) and a hair of negative body tracking + tighter `h1–h3` tracking for a more refined type feel; gave the canonical card surfaces (`.glass`, `.crm-surface-section`) a **subtle resting elevation** (were flat/border-only) so cards lift off the paper canvas consistently, with a gentle `translateY(-1px)` + deeper shadow on `.glass-hover`; upgraded the `.focus-ring` to a softer on-brand ring **with an accent glow**; softened the heavy single-layer popover/menu shadows (`.popover-surface`, `.shadow-pop`) into a **2-layer** premium depth; and added smooth, reduced-motion-aware transitions on interactive chrome (`a`, `button`, `[role=button]`). Direction: "premium refinement" (keep the look, elevate the polish) — no palette or layout changes. CSS-only; typecheck 0 · build 0 (tests unaffected). Archivos: app/globals.css, CHANGELOG.md.
- [2026-07-16 22:15 UTC] Design (round 2): **more premium polish, still token/global.** In `app/globals.css`: on-brand text `::selection` (accent tint), thin rounded WebKit **scrollbars** for horizontal tracks (tables/kanban) instead of the chunky OS default (vertical stays hidden by design), and motion-safe `scroll-behavior: smooth` for anchor jumps / scrollIntoView. Confirmed the shared `Input` (uses the now-glowing `.focus-ring`) and `Modal` (already `backdrop-blur-md`) needed no changes. CSS-only; build 0. Archivos: app/globals.css, CHANGELOG.md.
- [2026-07-16 22:30 UTC] Design (round 3, audit-driven): **premium consistency pass across shared components.** Ran a design-quality audit of the app and fixed the highest-impact "cheap" spots: **Toast** was styled by dead `styles/globals.css` classes (`toast-surface`/`toast-*`) so it had **no surface at all** — rebuilt on live tokens (white card + hairline border + per-type accent left edge + `shadow-pop`); **Tabs** active pill now lifts as a white `bg-surface-1 shadow-sm ring` chip instead of a flat tinted rectangle; **StatCard** dropped its `shadow-float` double-shadow so `.glass` owns the (now consistent) resting elevation; **SearchableSelect** + **DropdownMenu** popovers adopt the canonical `.popover-surface` (layered shadow) instead of ad-hoc `shadow-lg`; the shortcuts overlay backdrop moved from `bg-black/40` to the tokenized `bg-surface-0/80 backdrop-blur-md`; ad-hoc `animate-pulse` loading blocks in Dashboard + Tickets now use the `.skeleton` **shimmer**; and the paper canvas (`--color-surface-0`) was deepened a touch so white cards read as more lifted. typecheck 0 · lint clean · tests 2696/2696 · build 0. Deferred (follow-up): migrating Tickets' native `<select>`s to the `Select` component and routing plain empty states through the shared `EmptyState`. Archivos: components/ui/{Toast,Tabs,StatCard,SearchableSelect,DropdownMenu}.tsx, components/layout/Layout.tsx, views/{Dashboard,Tickets}.tsx, app/globals.css, styles/tokens.css, CHANGELOG.md.
- [2026-07-16 22:45 UTC] Design follow-up: **routed the detail-view empty states through the shared `EmptyState`.** `ContactDetail` (activities/deals/emails tabs) had bare gray-text empties with no icon; `CompanyDetail` (contacts/deals/activities/emails tabs) had hand-rolled icon+text blocks. Both now use `EmptyState` (compact density, icon in a rounded chip) inside the glass card, so every empty state reads consistently and premium. typecheck 0 · tests 2696/2696 · build 0. Remaining (lower-traffic, noted): Reports/Admin table empties, and Tickets' inline native `<select>`s (kept native by design — the app `Select` is the searchable panel, overkill for 4–5-option quick actions). Archivos: views/ContactDetail.tsx, views/CompanyDetail.tsx, CHANGELOG.md.
- [2026-07-16 23:12 UTC] Design (Aurora UI, first take — NEEDS a human eye on the preview): **added a soft ambient "aurora" wash of brand light to the app canvas.** `app/globals.css` gets a fixed, `z-index:-1` `body::before` with three low-opacity radial gradients in brand green/mint/accent (tunable via `--aurora-1/2/3`, moderate 0.13/0.13/0.09 defaults; set to 0 to disable). To let it show through the app interior, the three opaque `bg-surface-0` content containers in `components/layout/Layout.tsx` are now transparent (the body canvas shows behind; `.crm-page` has no bg, cards are opaque `.glass` so they float on the wash with zero readability impact). Feasibility answer to "is it tokenized for aurora": **yes** — the RGB-triplet brand tokens drive the gradients and `backdrop-blur` is already available; a bolder glassmorphism (translucent cards) is the optional next step. NOTE: I could NOT self-validate visually — the auth screens paint their own opaque canvas and the interior is auth-gated, so this must be reviewed logged-in on the preview. CSS/layout-only; typecheck 0 · build 0 (tests unaffected). Archivos: app/globals.css, components/layout/Layout.tsx, CHANGELOG.md.
- [2026-07-17 22:00 UTC] Composer: **Recipient chips auto-commit on SPACE + a per-chip To/Cc/Bcc move menu (#407, merged + promoted to production).** In `RecipientChips`, a token now commits to a chip on SPACE once it looks like an address (no trailing `;` needed), and — because the space only commits once the draft already contains `@` — display-name typing ("John Smith <…>") is preserved. Each committed chip gains a small menu to **move** the recipient between To / Cc / Bcc without retyping. Files: components/email/composer/RecipientChips.tsx, ComposerHeader.tsx. Gate: typecheck 0 · targeted composer tests green · build 0. Archivos: components/email/composer/{RecipientChips,ComposerHeader}.tsx, CHANGELOG.md.
- [2026-07-17 22:35 UTC] Composer: **Gmail send-as alias picker — send from a chosen verified identity (#408, merged + promoted to production).** Accounts with 2+ verified send-as identities get a "From" picker in the composer; the chosen alias is threaded end-to-end as the MIME From on every send path — the browser-direct Gmail send, the `scheduled_emails` payload, and the server-side `email-runner` — and sanitized against header injection. Alias discovery uses Gmail `settings/sendAs` behind a new `gmail.settings.basic` scope (sending from a verified alias needs no new scope; only *listing* aliases does). **PENDING human action:** redeploy the edge functions and reconnect Gmail once to grant the new scope. Gate: typecheck 0 · targeted tests green · build 0. Archivos: services/gmailService.ts, services/emailProviders/*, store/emailStore.ts, components/email/composer/ComposerHeader.tsx, components/email/composer/EmailComposer.tsx, supabase/functions/_shared/{emailOutbound,google}.ts, supabase/functions/email-runner/index.ts, CHANGELOG.md.
- [2026-07-17 23:10 UTC] Inbox/Composer: **Five composer/inbox correctness bugs (#409, merged + promoted to production).** (a) Inbox single-key shortcuts (e/#/r/f/u) no longer fire destructive thread actions while an inline reply is open (the guard now matches the composer region by `aria-labelledby`, not just `role=dialog`); (b) collapsing the Cc/Bcc/Reply-To rows no longer silently deletes typed recipients; (c) pasted inline images survive an autosave close/reopen; (d) removed a `ThreadView` effect that clobbered in-progress contact/deal link picks; (e) the Sent/Scheduled quick-filter chips are relabeled **Tracked/Opened** (new `inbox.tracked` / `inbox.opened` keys across all 6 locales — copy task, parity tsc-enforced). Gate: typecheck 0 · targeted tests green · build 0. Archivos: components/email/composer/*, features/inbox/*, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, CHANGELOG.md.
- [2026-07-17 23:45 UTC] Front End (a11y): **Accessible names for 16 icon-only buttons (#410, merged + promoted to production).** Added `aria-label`s to icon-only controls across SettingsPipelinesPanel, SalesGoals, PipelineTimeline, EmailTemplates, FollowUps, Notifications, AttachmentsList and Admin, so screen readers announce them. Gate: typecheck 0 · targeted tests green · build 0. Archivos: components/settings/SettingsPipelinesPanel.tsx, views/SalesGoals.tsx, components/deals/PipelineTimeline.tsx, components/email/EmailTemplates.tsx, components/**/FollowUps*.tsx, components/**/Notifications*.tsx, components/**/AttachmentsList.tsx, views/Admin.tsx, CHANGELOG.md.
- [2026-07-18 00:20 UTC] Clean code: **Centralized recipient-address splitting + deleted dead UI primitives (#411, merged + promoted to production).** Extracted `splitAddressTokens` (splits on comma OR semicolon) into `utils/outboundEmailIdentity` and used it everywhere the composer parses recipients — send path, draft save, `RecipientChips`, and the move-between-fields menu (previously send/save split on `,` only, so semicolon-separated lists broke). Deleted the unused UI primitives `PillToggle`, `SegmentedControl`, `Radio` (+ their tests). Gate: typecheck 0 · targeted tests green · build 0. Archivos: utils/outboundEmailIdentity.ts, components/email/composer/*, components/ui/{PillToggle,SegmentedControl,Radio}.tsx (deleted) + tests, CHANGELOG.md.
- [2026-07-18 00:55 UTC] Front End: **Button unification — 18 hand-rolled `btn-gradient` primaries → the shared `<Button>` (#412, merged + promoted to production).** Replaced ad-hoc gradient buttons in Sequences, EmailTemplates, TeamManagement, SalesGoals and CSVImport with the shared `<Button>` component, fixing drifting geometry (some were `rounded-full`, some `rounded-xl`). Gate: typecheck 0 · targeted tests green · build 0. Archivos: views/Sequences.tsx, components/email/EmailTemplates.tsx, views/TeamManagement.tsx, views/SalesGoals.tsx, components/import/CSVImport.tsx, CHANGELOG.md.
- [2026-07-18 01:30 UTC] Front End: **PageHeader migration for 9 screens (#413, merged + promoted to production).** Moved Campaigns, Tasks, Sequences, Tickets, TeamManagement, BookingPages, Duplicates, Admin and AuditLog onto the shared `PageHeader`, fixing header misalignment; the per-screen title becomes `sr-only` (the visible title is the topbar route). Gate: typecheck 0 · targeted tests green · build 0. Archivos: views/{Campaigns,Tasks,Sequences,Tickets,TeamManagement,BookingPages,Duplicates,Admin,AuditLog}.tsx, CHANGELOG.md.
- [2026-07-18 02:10 UTC] Front End: **Branded ConfirmDialog replaces every `window.confirm` (#414, merged + promoted to production).** New promise-based `useConfirm()` hook (`hooks/useConfirm.tsx`) renders the branded, focus-trapped, i18n `ConfirmDialog` through a `document.body` portal (so a transformed/overflow-hidden ancestor like the composer can't clip it). Migrated all **23** native `window.confirm` / global `confirm()` call sites across 17 files to it. Gate: typecheck 0 · targeted tests green · build 0. Archivos: hooks/useConfirm.tsx, components/ui/ConfirmDialog.tsx, + 17 call-site files, CHANGELOG.md.
- [2026-07-18 02:50 UTC] Perf: **Cache Intl formatters + a handful of hot-path wins (#415, merged + promoted to production).** `utils/formatters.ts` now caches `Intl.NumberFormat`/`DateTimeFormat` instances behind a keyed Map (was constructing a fresh formatter on every call, which runs per-row across deals/kanban/activities/detail views; output is byte-identical). Leads: skip prefetching score insights already cached (was ~15 redundant requests per keystroke). Companies: compute duplicate groups on-demand, not on every store change. Activities: filter before sort. Gate: typecheck 0 · targeted tests green · build 0. Archivos: utils/formatters.ts, views/Leads.tsx, store/companiesStore.ts, store/activitiesStore.ts (and consumers), CHANGELOG.md.
- [2026-07-18 03:30 UTC] Clean code: **Deleted dead `lib/posthog-server.ts` + reused `getInitials()` (#416, merged + promoted to production).** Removed `lib/posthog-server.ts` (0 references, the sole importer of `posthog-node`) and pointed lead avatars at the shared `getInitials()` helper instead of a local re-implementation. Gate: typecheck 0 · targeted tests green · build 0. Archivos: lib/posthog-server.ts (deleted), views/Leads.tsx (+ related), CHANGELOG.md.
- [2026-07-18 04:15 UTC] Front End: **Split two oversized files under the ~500-line rule (#417, merged + promoted to production).** `views/Calendar.tsx` 1416→497 (sub-views + date helpers extracted to `views/calendar/*`) and `views/Admin.tsx` 1225→771 (OrgDetailModal, PlanModal, badges and shared types extracted to `views/admin/*`). Admin's tab-panel bodies stay in the shell (no clean prop boundary yet), so it lands at 771, not <500 — tracked as follow-up. Gate: typecheck 0 · targeted tests green · build 0. Archivos: views/Calendar.tsx, views/calendar/*, views/Admin.tsx, views/admin/*, CHANGELOG.md.
- [2026-07-18 05:30 UTC] Front End: **Regression fixes from an adversarial bug-hunt over #407–#417 (#418, merged + promoted to production).** (a) `ThreadView` link picker: re-seed the pickers when the derived match resolves AFTER the thread was selected (`fetchThreadLinks`/contacts arriving late on first load), guarded by a dirty ref so it never clobbers an in-progress edit — closes a first-load data-loss path where "Save link" could overwrite the real link. (b) Companies/Contacts duplicate panel: recompute after a merge so the open panel drops the merged group instead of referencing a deleted record. (c) The formatter date-cache key now includes the options object. NOTE: across #407–#418 each PR passed the local gate (tsc + targeted tests + build; full suite ends at **2726 passing**); GitHub Actions CI instant-failed on exhausted account minutes every time (not real failures — the local gate was authoritative). Gate: typecheck 0 · full suite 2726 green · build 0. Archivos: features/inbox/**/ThreadView*.tsx, views/{Companies,Contacts}.tsx, utils/formatters.ts, CHANGELOG.md.
- [2026-07-18 06:10 UTC] Front End: **Cookie-preferences management UI + consent-withdrawal wiring (#419, merged + promoted to production).** The `CookieConsent` banner only ever showed once, so a visitor could never change their choice and `disablePostHog()` had no trigger. Added a re-openable **preferences mode** (opened via `openCookiePreferences()`, wired to a new "Cookie settings" link in the marketing footer — present on every marketing page incl. `/cookies`); switching "all"→"essential" now calls `disablePostHog()` (opt-out + reset), so consent can be withdrawn as easily as it was given (LSSI-CE / ePrivacy). New marketing i18n keys `settings`/`title` across all 6 locales. Gate: typecheck 0 · i18n-lint OK · CookieConsent tests 6/6 (+2) · build 0. Archivos: components/marketing/{CookieConsent,chrome}.tsx, i18n/marketing/{types,en,es,pt,fr,de,it}.ts, tests/components/marketing/CookieConsent.test.tsx, CHANGELOG.md.
- [2026-07-18 08:40 UTC] Front End: **App scale + width + composer compaction (#421–#425, merged + promoted to production).** Unified the page-width cap and went full-bleed, guaranteed the composer always shows a visible text box (it could squeeze to zero height), and shrank the whole app's root font-size in steps (90% → 85% → 70%) so more fits on screen without horizontal scroll. Gate per PR: typecheck 0 · build 0. Archivos: app/globals.css, components/email/composer/{ComposerBody,ComposerHeader,EmailComposer}.tsx.
- [2026-07-18 10:52 UTC] Perf: **Lighter login boot — cut the critical-path request burst (#426–#429, merged + promoted to production).** Trimmed the first-paint request burst on login, projected only the list columns actually rendered for companies/deals/activities (smaller PostgREST payloads), moved custom-field *values* to lazy per-entity loads instead of one org-wide fetch, and deferred companies off the critical first-paint burst. Gate per PR: typecheck 0 · targeted store/hook tests green · build 0. Archivos: components/layout/Layout.tsx, hooks/useDataInit.ts, store/{activitiesStore,companiesStore,dealsStore,customFieldsStore}.ts, components/shared/CustomFieldRenderer.tsx, tests/stores/*.
- [2026-07-18 12:05 UTC] Back End: **Public API v1.1 → v1.4 + TypeScript SDK (#430–#434, #444, merged + promoted to production).** v1.1 richer reads, bulk writes and 2 new resources; v1.2 `Idempotency-Key` for POST (migration `20260718120000_api_idempotency`); v1.3 rate-limit response headers, incremental sync and cursor pagination; v1.4 nested reads, bulk delete and 2 more resources. Shipped a zero-dep ESM TypeScript SDK (`sdk/propel.ts`, packaged with README + package.json for publishing) plus expanded `docs/api-public.md` and the in-app `/docs` reference/guides (kept in sync with `openapi.json`). Gate per PR: typecheck 0 · publicApiQuery/SDK tests green · build 0. **PENDING human action:** deploy edge functions + apply the idempotency migration. Archivos: supabase/functions/_shared/{publicApiResources,publicApiQuery,publicApiOpenApi,rateLimit,cors}.ts, supabase/functions/public-api/index.ts, supabase/migrations/20260718120000_api_idempotency.sql, sdk/{propel.ts,README.md,package.json,tsconfig.json}, docs/api-public.md, views/DocsPortal.tsx, views/docs/{apiReference,apiGuides}.tsx, tests/{publicApiQuery,sdk/propel}.test.ts.
- [2026-07-18 12:57 UTC] Front End: **Responsive app scale + centered /docs on wide screens (#435–#436, merged + promoted to production).** App scale 70% → 75% and switched the root font-size to a fluid `clamp()` so the UI scales smoothly across viewport widths; centered the `/docs` content column on wide screens. Gate: typecheck 0 · build 0. Archivos: app/globals.css, views/DocsPortal.tsx.
- [2026-07-18 13:26 UTC] DevOps: **CI Deno type-check job for edge functions (#437, merged + promoted to production).** Added a CI job that type-checks `supabase/functions/**` with Deno (the frontend `tsc` excludes edge code), so edge regressions surface in CI, not only at deploy. Archivos: .github/workflows/ci.yml.
- [2026-07-18 13:31 UTC] Cybersec: **Revoke RPC-surface access to internal SECURITY DEFINER functions (#438, merged + promoted to production).** Hardened the RPC surface so internal `SECURITY DEFINER` helpers are not callable by the `authenticated`/`anon` roles over PostgREST (idempotent migration). **PENDING human action:** apply the migration. Archivos: supabase/migrations/20260718112925_rpc_surface_hardening.sql.
- [2026-07-18 14:21 UTC] Perf: **Dashboard + Reports server-side aggregation (#439–#441, merged + promoted to production).** New server-side aggregate RPC backs the Dashboard KPIs (layer 1) and then topDeals/revenue/recent/heatmap (layer 2), replacing client-side roll-ups over full tables; Reports now makes one consolidated `/analytics/report` call instead of 5. Gate per PR: typecheck 0 · hook/Reports tests green · build 0. **PENDING human action:** apply the `dashboard_metrics_rpc` migration. Archivos: hooks/useDashboardMetrics.ts, views/{Dashboard,Reports}.tsx, lib/supabase/analytics.ts, supabase/migrations/20260718114733_dashboard_metrics_rpc.sql, tests/hooks/useDashboardMetrics.test.ts, tests/views/Reports.test.tsx.
- [2026-07-18 14:41 UTC] Front End: **In-app cookie consent so PostHog can track in-app users (#442, merged + promoted to production).** The consent banner previously rendered only on the marketing site, so in-app-only users stayed un-tracked; added an in-app consent surface (Layout + a Settings → Data control) that live-boots/opts-out PostHog per the same `propel_cookie_consent` preference. Gate: typecheck 0 · build 0. Archivos: components/layout/Layout.tsx, views/settings/DataSection.tsx.
- [2026-07-18 15:01 UTC] Cybersec: **Stop the public feedback bucket allowing file listing (#443, merged + promoted to production).** Tightened the storage policy so the public feedback bucket no longer permits object listing (only the intended per-object access). **PENDING human action:** apply the migration. Archivos: supabase/migrations/20260718130051_feedback_bucket_no_listing.sql.
- [2026-07-18 17:58 UTC] Front End: **Reports/Dashboard UX + a11y + safer data import (#445, merged + promoted to production).** Rounded Reports percentages (no more 12.333333%), added missing a11y labels, and made the Settings → Data import path safer; new i18n keys across all 6 locales. Gate: typecheck 0 · i18n-lint OK · build 0. Archivos: views/{Dashboard,Reports}.tsx, views/settings/DataSection.tsx, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts.
- [2026-07-18 18:08 UTC] Test: **Cover the session's new analytics/dashboard/SDK/query code (#446, merged + promoted to production).** Added unit/component coverage for the dashboard-metrics hook + fetch path, the analytics helpers, the public-API query builder, the SDK, Reports, and Settings → Data. Gate: full suite green. Archivos: tests/hooks/{useDashboardMetrics,useDashboardMetricsFetch}.test.ts, tests/lib/analyticsExtra.test.ts, tests/publicApiQuery.test.ts, tests/sdk/propel.test.ts, tests/views/Reports.test.tsx, tests/views/settings/DataSection.test.tsx.
- [2026-07-18 19:08 UTC] Front End (i18n): **Close remaining translation gaps (#447–#449, merged + promoted to production).** Translated leaked English strings across es/pt/de/fr/it (app + marketing/fr + workflowLibrary), synced `<html lang>` in-app and honored the browser-language mode in the date/number formatters, and localized the last hardcoded in-app strings (support-session banner, form labels, toasts) with new keys across all 6 locales. Gate per PR: typecheck 0 · i18n-lint OK · formatter/locale tests green · build 0. Archivos: i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, i18n/marketing/fr.ts, i18n/workflowLibrary/{es,fr,it,pt}.ts, components/ClientApp.tsx, hooks/useDateLocale.ts, i18n/index.ts, utils/formatters.ts, + ~12 view/component call sites (support-session, ContactForm, composer, BookingPages, Campaigns, Contacts, Sequences, branding).
- [2026-07-18 19:53 UTC] AI (client): **Meeting-notes dedupe + persisted conversation + snoozable next-actions (#450–#451, merged + promoted to production).** Stopped Meeting Notes duplicating on save and recovered dropped extraction signals (`utils/meetingTranscript`), persisted the assistant conversation across reloads, and made Sales-Intelligence next-actions snoozable (`store/snoozedActionsStore`) so dismissed items stay dismissed. New i18n keys × 6 locales. Gate: typecheck 0 · aiStore/SalesIntelligence/snooze tests green · build 0. Archivos: components/ai/{AiAssistant,MeetingNotesDialog}.tsx, components/dashboard/SalesIntelligence.tsx, store/{aiStore,snoozedActionsStore}.ts, utils/meetingTranscript.ts, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, tests/*.
- [2026-07-18 20:16 UTC] AI (edge): **Sharper, token-lean agent + depth tools (#452–#453, merged + promoted to production).** Reworked the agent system prompt to be more individualized and token-lean, and gave it `get_deal` / `get_contact` depth tools so it can drill into a specific record instead of re-listing. Both are RLS-scoped (mutations still gated by `allowWrites` + `ctx.role`). **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/aiAgent.ts, supabase/functions/_shared/handlers/ai.ts.
- [2026-07-18 20:45 UTC] AI (client): **Editable action draft + safe markdown replies (#454–#455, merged + promoted to production).** The assistant now shows an **editable draft** of a proposed write action before you approve it (change the field values, then confirm), and renders replies as **sanitized markdown** with an `aria-live` region for screen readers (`utils/assistantMarkdown`). New i18n keys × 6 locales. Gate: typecheck 0 · aiStore/markdown tests green · build 0. Archivos: components/ai/AiAssistant.tsx, utils/assistantMarkdown.ts, store/aiStore.ts, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, tests/*.
- [2026-07-18 21:46 UTC] AI: **Governance UI + per-user instructions + copy button + conversation history (#456–#460, merged + promoted to production).** Added **Settings → AI** to govern the org assistant (allow-writes default, etc.); **per-user AI instructions** that are server-persisted so they follow the user cross-device (client cap 500 chars, migration `20260718140000_profiles_ai_instructions`, threaded into the agent as `userInstructions`); a **copy button** on assistant replies; and **conversation history** (browse + resume past chats, titles derived from the first message). Gate per PR: typecheck 0 · aiStore/AiSection/AiAssistant tests green · build 0. **PENDING human action:** deploy edge functions + apply the profiles.ai_instructions migration. Archivos: views/settings/{AiSection,SettingsPage}.tsx, components/ai/AiAssistant.tsx, store/aiStore.ts, supabase/functions/_shared/{aiAgent.ts,handlers/ai.ts}, supabase/migrations/20260718140000_profiles_ai_instructions.sql, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, tests/*.
- [2026-07-18 22:17 UTC] Cybersec (edge): **Guard admin password-reset by role + count OpenAI-compat tokens (#461, merged + promoted to production).** The admin reset-password handler now loads the target's `role`/`is_super_admin` and refuses to reset a super-admin, or an owner by a non-owner (privilege-hierarchy guard); the OpenAI-compatible agent path now accounts for token usage like the Gemini path. **PENDING human action:** deploy edge functions (inert until deployed). Archivos: supabase/functions/_shared/handlers/account.ts, supabase/functions/_shared/aiAgent.ts.
- [2026-07-18 22:35 UTC] Front End: **Focus-steal + bulk-send resilience fixes (#462–#463, merged + promoted to production).** Modal/SlideOver/ConfirmDialog no longer steal focus back from their own inputs on every render (focus effect keyed on open-state, close handler read via ref); bulk email now uses `allSettled` so one failed recipient doesn't abort the batch, and escapes the HTML body. New i18n keys × 6 locales. Gate: typecheck 0 · Modal/enqueueBulkSends tests green · build 0. Archivos: components/ui/Modal.tsx, features/inbox/enqueueBulkSends.ts, views/Contacts.tsx, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, tests/*.
- [2026-07-18 22:51 UTC] AI (client): **Drop stale results + role-aware empty-state suggestions (#464–#465, merged + promoted to production).** The assistant and `AiInsight` now drop a result if the user switched context (deal/contact) before it arrived (no more answer from the previous record), and the empty-state suggestion chips are **role-aware** (a sales_rep and an admin see different starting prompts). New i18n keys × 6 locales. Gate: typecheck 0 · AiInsight/aiStore/AiAssistant tests green · build 0. Archivos: components/ai/{AiAssistant,AiInsight}.tsx, store/aiStore.ts, i18n/{en,es,pt,de,fr,it}.ts, i18n/types.ts, tests/*.
- [2026-07-19 00:07 UTC] Perf: **Boneyard — keep-alive route outlet for instant page loads (#466, merged + promoted to production).** New `KeepAliveOutlet` caches the rendered element of param-less routes (dashboard, contacts, leads, companies, deals, activities, inbox, campaigns, calendar, analytics, sequences, products, tickets, timeline, goals) in an LRU of 5, hiding parked panes with `display:none` instead of unmounting so revisiting a screen is instant (no refetch/re-render). Parked panes get a frozen react-router `LocationContext` (so they don't react to unrelated nav or read the wrong params); a live `KeepAliveActiveContext` re-sets the page title and suppresses parked panes' body-portal modals; per-route scroll position is saved/restored on the shared `#main-content`. Gate: typecheck 0 · KeepAliveOutlet tests green (parked/no-remount, non-cacheable remounts, modal suppression) · build 0. Archivos: components/layout/KeepAliveOutlet.tsx, contexts/KeepAliveContext.ts, components/ClientApp.tsx, components/ui/Modal.tsx, tests/components/layout/KeepAliveOutlet.test.tsx.
- [2026-07-19 00:16 UTC] Perf: **Batch campaign metrics + memoize inbox deals projection (#467, merged + promoted to production).** Campaigns fetched delivery metrics per-card (N+1); now one batched pair of requests (chunks of 50) fans out into a per-campaign map. Inbox no longer rebuilds the `{id,title}` deals projection inline on every render (memoized). Gate: typecheck 0 · campaignsStore tests green · build 0. Archivos: store/campaignsStore.ts, views/{Campaigns,inbox/InboxPage}.tsx, tests/stores/campaignsStore.test.ts.
- [2026-07-19 00:25 UTC] Back End: **Six edge bugs from the second adversarial sweep (#468, merged + promoted to production).** email-runner: strip client-supplied `headers` + CRLF-sanitize subject/to/cc/bcc/replyTo (header-injection). public-booking: cancel selects `organization_id`, lead insert uses the invitee notes. public-api: sanitize the search term before building the `.or()` filter. stripe-webhook: persist `trial_ends_at` on subscription sync. billing: refuse a second Checkout when the org already holds a live subscription (409) to avoid a silent double-charge. **PENDING human action:** deploy edge functions (inert until deployed). Archivos: supabase/functions/_shared/handlers/billing.ts, supabase/functions/_shared/publicApiResources.ts, supabase/functions/email-runner/index.ts, supabase/functions/public-booking/index.ts, supabase/functions/stripe-webhook/index.ts.
- [2026-07-19 01:05 UTC] Front End: **Surgical per-row rollback + re-derive org on token refresh (#470, merged + promoted to production).** Third adversarial sweep, client half. `dealsStore.updateDeal`/`deleteDeal` and `leadsStore.updateLead` rolled back to a whole-list snapshot, so during a parallel bulk op (handleBulkAssign / handleBulkDeleteDeals / bulk stage-assign-tag, all `Promise.all(ids.map(...))`) one failure clobbered every concurrent SUCCESS — reverting siblings the server had saved, or resurrecting deals the server had deleted (phantom rows that 404). Now revert/re-insert only the failed row, matching moveDeal/reorderDeal. `authStore` TOKEN_REFRESHED re-synced only `expiresAt`, never re-deriving `organizationId`; the 2-hour support-session auto-expiry reissues a JWT whose org_id reverts to the operator's own org with no reload, leaving the store on the impersonated org (INSERTs 42501, stale "viewing org X" banner) — now detect the org_id claim flip and force a reload. Gate: typecheck 0 · i18n-lint OK · 2858 tests green (+3) · build 0. Archivos: store/{dealsStore,leadsStore,authStore}.ts, tests/stores/{dealsStore,leadsStore}.test.ts.
- [2026-07-19 01:20 UTC] Back End (edge): **AI agent tool correctness — stage resolution, `.or()` sanitization, contact-status allow-list (#471, merged + promoted to production).** Third sweep, edge half. `update_deal_stage` wrote the model-supplied stage NAME into `deals.stage` (which holds a pipeline stage ID) — "mark Acme won" wrote `Won` not `closed_won`, dropping the deal off the Kanban board and corrupting won/lost metrics; now resolves name→id against the deal's pipeline like `create_deal`. Added an `orLike()` helper stripping the PostgREST `.or()` metacharacters (`, ( ) " \`) + LIKE wildcards, applied to all nine `.or()` search sites (a comma/paren in the query used to 400 or inject OR conditions). `update_contact` now validates `status` against prospect/customer/churned (contacts.status has no DB CHECK). **PENDING human action:** deploy edge functions (inert until deployed). Archivos: supabase/functions/_shared/aiAgent.ts.
- [2026-07-19 01:35 UTC] Cybersec: **RBAC gap — role-gate `pipelines`/`automation_executions`/`pipeline_members` (#472, merged + promoted to production).** Third sweep, RLS half. The RBAC hardening migration (20260702010000) left these three tables off every tier array, so they kept their role-blind `FOR ALL` policy and any org member — including a read-only `viewer` — could INSERT/UPDATE/DELETE them via direct PostgREST (a viewer could delete the default pipeline, dropping every deal off the board, or forge automation-execution logs, or self-add into a members-only pipeline). New idempotent migration gives them the same `jwt_can_write()` gating the comparable tier-1 tables already have (pipeline_members scoped through its parent pipeline's org). **PENDING human action:** apply the migration (`supabase db push`). Archivos: supabase/migrations/20260718150000_rbac_pipelines_hardening.sql, CHANGELOG.md.
- [2026-07-19 02:05 UTC] Back End (edge): **Self-review follow-up on the sweep-3 AI-agent fix (#473, merged + promoted to production).** An adversarial re-verification of #471 caught two regressions in that same fix: (1) `orLike()` can collapse an all-metacharacter query to `''`, so `ilike.%%` would match EVERY row — on the `.limit(1)` write/lookup tools (update_contact, update_lead_status, convert_lead, and the create_deal/schedule_meeting contact links) that would mutate/link an ARBITRARY record instead of failing safe; added an empty-term guard to every `.or()` search site (reads return empty, write lookups return no-match, optional links are skipped). (2) `update_deal_stage` swallowed the pipelines-fetch error and, on an empty/failed stages read, fell back to writing the raw stage name again (re-opening the corruption); now it surfaces the error and refuses to write a raw name for a deal that HAS a pipeline. **PENDING human action:** deploy edge functions (inert until deployed). Archivos: supabase/functions/_shared/aiAgent.ts, CHANGELOG.md.
- [2026-07-19 03:00 UTC] Document: **Spec — Meeting Notetaker (self-hosted bot), Phase 2/3 of AI Meeting Notes (#474).** Prescriptive design for a Fathom/Read.ai-style AI notetaker: a **visibly-named self-hosted bot** (Attendee/Vexa, not a managed API — EU residency, no per-minute COGS) joins the user's Zoom/Meet/Teams call, records+transcribes, and Propel turns the transcript into a meeting activity with summary + action items **auto-attached to the matched deal** (attendee email → contact → open deal). Vendor-agnostic boundary (one adapter file swaps Attendee↔Vexa); reuses the existing paste-transcript notes pipeline, `calendar_events`, and the zoom-webhook/resend-webhook patterns. Covers the org-scoped `meeting_sessions`/`meeting_transcripts` schema (RLS + cascade FKs + a `purge_meeting_transcripts` retention cron), the `meeting-webhook` ingest (HMAC, accept-and-log until secret) + `/meetings/*` dispatcher (503 until configured, `assertNotImpersonatingOutbound`-gated), consent-by-visible-bot compliance, a 3-phase plan (Phase A ships inert/dark), and what the operator must provision. Archivos: docs/specs/meeting-notetaker.md, CHANGELOG.md.
- [2026-07-19 03:30 UTC] Back End: **Meeting Notetaker Phase A — session/transcript schema + retention (#475).** Foundation for the self-hosted notetaker (spec docs/specs/meeting-notetaker.md): org-scoped `meeting_sessions` + `meeting_transcripts` (RLS tier-1 read=member / write=jwt_can_write, `on delete cascade` FKs to org/contact/deal + set-null to profiles/calendar_events/activities), a unique `(organization_id, external_bot_id)` dedup index, and `purge_meeting_transcripts(retain_days=90)` on a nightly pg_cron (mirrors `purge_ai_transcripts`). Ships dark — no runtime references the tables until the edge layer + secrets land. **PENDING human action:** apply the migration (`supabase db push`). Archivos: supabase/migrations/20260719000000_meeting_notetaker.sql, CHANGELOG.md.
- [2026-07-19 04:00 UTC] Back End (edge): **Meeting Notetaker Phase A — ingest webhook + read API (#476).** New `meeting-webhook` edge fn (one endpoint per org `…/meeting-webhook/<orgId>`, `verify_jwt=false`, HMAC over the raw body via `MEETING_BOT_WEBHOOK_SECRET`, **accept-and-log until that secret is set** — email-track/resend-webhook pattern), a **vendor-neutral** boundary (`_shared/meetingBot/{types,generic,index,ingest}.ts`: a `MeetingBotEvent` normal form + a `generic` adapter that understands Propel's own envelope, so Attendee/Vexa native adapters land in Phase B as one file each), and `propel-api` `/meetings/sessions` read/manage endpoints (`handlers/meetings.ts`, RLS-scoped, list/detail+transcript/delete). Ingest stores session status + raw transcript org-scoped; notes + deal-matching are Phase B. Ships dark (no secret → accept-and-log). **PENDING human action:** deploy edge functions. Archivos: supabase/functions/meeting-webhook/index.ts, supabase/functions/_shared/meetingBot/*, supabase/functions/_shared/handlers/meetings.ts, supabase/functions/_shared/handlers.ts, supabase/functions/propel-api/index.ts, supabase/config.toml, CHANGELOG.md.
- [2026-07-19 04:45 UTC] Back End (edge): **Meeting Notetaker Phase B-1 — Attendee adapter + dispatcher (#477).** Vendor chosen: **Attendee** (attendee-labs/attendee). New `_shared/meetingBot/attendee.ts` wires the real contract (confirmed against the repo + docs/webhooks.md): dispatch `POST /api/v1/bots` (`Authorization: Token`), webhook signature `X-Webhook-Signature = base64(HMAC-SHA256(base64decode(secret), rawBody))`, `bot.state_change` → status mapping with `event_type=post_processing_completed` as the transcript-ready signal, and out-of-band transcript fetch via `GET /api/v1/bots/{id}/transcript` (defensively normalized). Registered in `getMeetingBotAdapter()` on `MEETING_BOT_PROVIDER=attendee`; the webhook now fetches + appends the full transcript on completion. Added `POST /meetings/notetaker` dispatcher (503 until `MEETING_BOT_API_URL/KEY` set, write-role gated, `assertNotImpersonatingOutbound`, creates the session then sends the bot). Notes-extraction + attendee→contact→deal matching remain Phase B-2. **PENDING human action:** deploy edge functions + set MEETING_BOT_PROVIDER/API_URL/API_KEY/WEBHOOK_SECRET. Archivos: supabase/functions/_shared/meetingBot/{attendee,index,types}.ts, supabase/functions/meeting-webhook/index.ts, supabase/functions/_shared/handlers/meetings.ts, docs/specs/meeting-notetaker.md, CHANGELOG.md.
- [2026-07-19 05:10 UTC] Back End (edge): **Meeting Notetaker Phase B-2 — attach-to-deal endpoint (#478).** `POST /meetings/sessions/:id/attach` persists a session's notes into the CRM: creates the **meeting activity** (`type=meeting`, linked to the chosen deal/contact) + a **task** per action item, and links them back onto the session (`matched_deal_id`/`matched_contact_id`/`activity_id`, status→completed). Write-role gated, org-scoped. The summary itself is generated client-side via the existing `/ai/meeting-notes` pipeline (which owns AI governance + credit metering), so this endpoint stays a thin persistence step — no edge AI refactor. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/handlers/meetings.ts, CHANGELOG.md.
- [2026-07-19 05:40 UTC] Front End: **Meeting Notetaker — the Meeting Notes screen (#479, ships via Vercel).** New `/meetings` route + Sidebar nav (Sales), `store/meetingsStore.ts` + `views/MeetingNotes.tsx`: lists the org's notetaker sessions with status badges, opens a session in a SlideOver with its transcript, **Generate notes** (reuses the existing `/ai/meeting-notes` pipeline → summary + action items), a deal picker, and **Attach to deal** (→ `POST /meetings/sessions/:id/attach` → meeting activity + tasks). Empty state explains the notetaker; the feature stays inert until the bot is configured (dispatch 503s, list is empty). New `t.meetings.*` + `t.nav.meetings` across all 6 locales (es/pt fully localized; de/fr/it inherit the section via `...en`, nav label localized). Gate: typecheck 0 · i18n-lint OK · tests green (+5 meetingsStore) · build 0. Archivos: store/meetingsStore.ts, views/MeetingNotes.tsx, components/ClientApp.tsx, components/layout/Sidebar.tsx, i18n/{types,en,es,pt,de,fr,it}.ts, tests/stores/meetingsStore.test.ts, CHANGELOG.md.
- [2026-07-19 06:15 UTC] Cybersec (edge): **Meeting Notetaker — fix multi-tenant webhook routing + attach ownership + idempotency (#480).** Adversarial verification of the whole feature caught: (HIGH) the webhook derived the org from the URL path, but Attendee has a single global webhook config — so the documented setup ingested nothing, or (if an org id were pinned) misfiled one org's transcript under another. Now the tenant is resolved inside `ingest` from the globally-unique bot id (`meeting_sessions.external_bot_id → organization_id`); a bot Propel never dispatched is dropped (no cross-tenant write). (MED) `/attach` now validates `dealId`/`contactId` belong to the caller's org before stamping them onto activities (CLAUDE §4 boundary authz). (MED) idempotency: a retried `post_processing_completed` no longer duplicates the transcript row (skip if the session already has one). (LOW) error responses standardized on `Response.json` (Content-Type); documented the deliberate `assertPublicHttpsUrl` omission for the private self-hosted bot host; spec routing/retention notes corrected. **PENDING human action:** deploy edge functions (point Attendee's single webhook at `…/functions/v1/meeting-webhook`, no org segment). Archivos: supabase/functions/meeting-webhook/index.ts, supabase/functions/_shared/meetingBot/ingest.ts, supabase/functions/_shared/handlers/meetings.ts, docs/specs/meeting-notetaker.md, CHANGELOG.md.
- [2026-07-19 06:30 UTC] Front End: **Meeting Notetaker — client self-review fixes (#481, ships via Vercel).** From the same verification pass: added `/meetings` to `NAV_PERMISSIONS` (`activities:read`) so the sidebar item is permission-gated like every other route; deleting a session from the detail SlideOver now closes the panel (was left open on the just-deleted row); the "Open meeting" link only renders when `safeHref` returns a value (no href-less anchor); and `generate()` surfaces errors via the shared `getErrorMessage` helper. Gate: typecheck 0 · i18n-lint OK · tests green · build 0. Archivos: utils/permissions.ts, views/MeetingNotes.tsx, CHANGELOG.md.
- [2026-07-19 06:55 UTC] Back End (edge): **Meeting Notetaker Phase C — calendar auto-dispatch engine (#482).** New `meeting-runner` pg_cron edge fn (verify_jwt=true, service-role, `isServiceRoleCaller` + `?dryRun`, same pattern as automation-runner/email-runner): for orgs that enable `settings.app.meetingAutoDispatch`, it sends a notetaker bot to `calendar_events` with a `meet_link` starting in the LEAD/BACKFILL window — the "it just shows up" behavior — creating one `meeting_session` per `calendar_event_id` (created before dispatch so a retried tick dedups). Capped (50/run, 20/org). Scheduled every 5 min via `propel-meeting-runner` cron (migration `20260719010000`, Vault key pattern). **Dark by default:** no-op until the bot API is configured AND an org opts in. **PENDING human action:** deploy edge functions + apply the cron migration. Archivos: supabase/functions/meeting-runner/index.ts, supabase/migrations/20260719010000_meeting_runner_cron.sql, supabase/config.toml, CHANGELOG.md.
- [2026-07-19 07:15 UTC] Front End: **Meeting Notetaker Phase C — auto-dispatch toggle (#483, ships via Vercel).** An admin/owner "Auto-record my meetings" Switch in the Meeting Notes header flips `settings.app.meetingAutoDispatch` (via the existing `/orgs/me/settings` PATCH + settingsStore, optimistic + rollback), which the `meeting-runner` cron reads to auto-send a notetaker bot to calendar events. New `AppSettings.meetingAutoDispatch` (default false) hydrated from the server + reset on data-reset; new `t.meetings.autoDispatch` (en/es/pt; de/fr/it inherit). Gate: typecheck 0 · i18n-lint OK · tests green · build 0. Archivos: types/index.ts, utils/defaultAppSettings.ts, store/settingsStore.ts, views/MeetingNotes.tsx, i18n/{types,en,es,pt}.ts, CHANGELOG.md.
- [2026-07-19 08:00 UTC] Back End + Front End: **Meeting Notetaker — auto-dispatch is now per-USER, not per-org (#484, merged + promoted).** New `profiles.meeting_auto_dispatch` (per-user opt-in) supersedes the org-level `settings.app.meetingAutoDispatch` from #483: each teammate decides whether the bot joins THEIR meetings. The `meeting-runner` cron reads the per-user flag; the Meeting Notes toggle now writes the profile column. **PENDING human action:** apply the migration + deploy edge functions. Archivos: supabase/migrations/*_profiles_meeting_auto_dispatch.sql, supabase/functions/meeting-runner/index.ts, store/*, views/MeetingNotes.tsx, CHANGELOG.md.
- [2026-07-19 08:30 UTC] Document: **Attendee self-host runbook for the notetaker (#485).** Operator guide: provision the self-hosted Attendee bot, set `MEETING_BOT_*` secrets, apply the migrations + deploy, point Attendee's single global webhook at `…/meeting-webhook`, smoke-test, and enable the per-user auto-dispatch toggle. Archivos: docs/runbooks/meeting-notetaker-attendee.md, CHANGELOG.md.
- [2026-07-19 09:00 UTC] Back End (edge): **Notetaker state-machine + idempotency hardening (#486).** Adversarial review of the just-shipped feature: `is_final` column + partial unique indexes so a retried terminal webhook can't duplicate a transcript, tightened state transitions. **PENDING human action:** apply the migration (`20260719030000`) + deploy edge functions. Archivos: supabase/migrations/20260719030000_meeting_notetaker_hardening.sql, supabase/functions/meeting-webhook/index.ts, CHANGELOG.md.
- [2026-07-19 09:20 UTC] Back End (edge): **Clear diagnostic on a non-base64 webhook secret (#487).** The HMAC verify silently failed when `MEETING_BOT_WEBHOOK_SECRET` wasn't valid base64; now logs an actionable diagnostic instead. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/meeting-webhook/index.ts, CHANGELOG.md.
- [2026-07-19 10:00 UTC] Front End + Back End (edge): **Live transcription during the call — Phase C (#488).** The notetaker now shows a growing transcript while the call is in progress (interim, non-final segments), not only after it ends. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/meetingBot/*, views/MeetingNotes.tsx, store/meetingsStore.ts, CHANGELOG.md.
- [2026-07-19 10:40 UTC] Back End (edge): **public-api billing-lock sync + ref-ownership + proto guards (#489).** Migration `20260719040000` realigns the DB plan-limit trigger's lock definition that had drifted from the edge check; referenced-id ownership validated at the boundary; prototype-pollution guards on body parsing. **PENDING human action:** apply the migration + deploy edge functions. Archivos: supabase/migrations/20260719040000_limits_trigger_sync_locked.sql, supabase/functions/_shared/publicApi*, CHANGELOG.md.
- [2026-07-19 11:20 UTC] Front End: **Talk-time analytics in the meeting detail — Phase C (#490).** Talk-time bars under the transcript (final transcripts only), derived client-side from the speaker segments. Feeding talk-time/sentiment into `utils/dealHealth` + Sales Intelligence is noted as a follow-up (needs a live Attendee run). Archivos: views/MeetingNotes.tsx, utils/*, CHANGELOG.md.
- [2026-07-19 12:00 UTC] Front End: **Perf — de-quadratic Sales Intelligence + indexed user lookups (#491).** Removed an O(n²) pass in the Sales Intelligence engine and replaced repeated user linear-scans with a keyed map. Archivos: features/intelligence/*, components/dashboard/SalesIntelligence.tsx, CHANGELOG.md.
- [2026-07-19 12:40 UTC] Back End (edge): **public-api blocks writes for a billing-locked org, reads stay open (#492).** A lapsed/blocked subscription now 402s public-API writes while GETs keep working (read-only degrade). **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/publicApiResources.ts, CHANGELOG.md.
- [2026-07-19 13:10 UTC] Back End (edge): **Escape invitee HTML + strip CR/LF from name in booking emails (#493).** The booking confirmation email interpolated the raw invitee name into HTML (stored-XSS vector) and into headers (injection); now HTML-escaped + CRLF-stripped + length-bounded. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/public-booking/index.ts, CHANGELOG.md.
- [2026-07-19 13:50 UTC] Back End (edge) / Cybersec: **Harden marketing/compliance surface (#494).** `email-runner buildMime` strips CR/LF + quotes from attachment name/mimeType; Resend-webhook HMAC/Svix verification tightened; suppression-list checks + MIME assembly reviewed. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/email-runner/index.ts, supabase/functions/_shared/emailCompliance.ts, CHANGELOG.md.
- [2026-07-19 14:30 UTC] Back End (edge): **Server-side write-gate in `runTool` + wildcard-guard write matchers (#495).** The AI agent's tool dispatcher now re-checks write permission server-side inside `runTool` (defence in depth beyond the tool-registration gate), and the write-tool matchers are anchored so a wildcard can't slip a write tool through. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/aiAgent.ts, CHANGELOG.md.
- [2026-07-19 15:10 UTC] Cybersec (edge): **Enforce MFA step-up (aal2) at the edge, fail-open (#496).** Sensitive edge routes require the caller's JWT to carry `aal2` when the org mandates MFA; implemented fail-open (a missing/unknown AAL claim doesn't lock a legitimate user out) with a clear PR note. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/_shared/auth.ts, CHANGELOG.md.
- [2026-07-19 16:00 UTC] Front End: **Scale up density on large monitors + roomier feedback dialog (#497, ships via Vercel).** The global root font-size (the single per-viewport "zoom" knob) sat at ≈75% on large screens; raised the `clamp()` so the app reads at ~85%, and enlarged the feedback dialog (was cramped). Archivos: app/globals.css, components/**/Feedback*, CHANGELOG.md.
- [2026-07-19 16:40 UTC] Front End: **Full-height split views through the keep-alive outlet (#498, ships via Vercel).** The keep-alive wrapper collapsed content height, so `crm-page-full` split views (Deals kanban, inbox) didn't reach the bottom and showed a stray horizontal scrollbar; the wrapper is now `h-full` (still `display:none` when parked, so no effect on cached panes). Kanban now extends to the bottom. Archivos: components/layout/KeepAliveOutlet.tsx, CHANGELOG.md.
- [2026-07-19 17:20 UTC] Back End (edge) + Front End: **Brand the public booking page (#499).** `public-booking` GET now returns `organization { name, logoUrl, brandColor }` (https-/hex-validated, read from `organizations.settings.app.branding`); the booking info panel leads with the org logo (or a brand-coloured monogram) + name, duration/timezone become chips, and a brand accent stripe. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/public-booking/index.ts, views/PublicBooking.tsx, CHANGELOG.md.
- [2026-07-19 18:00 UTC] Front End: **Rule-based follow-up suggestions from email — no AI (#500, ships via Vercel).** A dismissible suggestions strip above the inbox proposes follow-ups: `features/inbox/suggestFollowUps.ts` (pure, tested) ranks CRM-linked threads awaiting a reply (inbound ≥1 day) or with no reply sent (own last message ≥3 days), skipping threads that already have a logged follow-up. New `t.inbox.suggest*` keys across all 6 locales. Archivos: features/inbox/suggestFollowUps.ts, components/inbox/InboxSuggestions.tsx, views/inbox/InboxPage.tsx, i18n/*, CHANGELOG.md.
- [2026-07-20 09:00 UTC] Back End (edge): **Booking availability interpreted in the page timezone, not UTC (#501).** A 09:00–17:00 window configured in Europe/Madrid rendered slots from 11:00 (a +2h CEST shift) because `computeSlots` treated the availability as UTC then displayed in the page tz. Now a DST-correct `zonedTimeToUtc(y,mo,d,hh,mm, page.timezone)` (via `Intl`) computes each slot, POST validation recomputes against the slot's LOCAL calendar date, and GET display + POST validation stay consistent. **PENDING human action:** deploy edge functions. Archivos: supabase/functions/public-booking/index.ts, CHANGELOG.md.
- [2026-07-20 10:00 UTC] Front End: **Quote Builder — always-visible field labels (#502, ships via Vercel).** Every config field (document type, quote number, VAT %, validity, tax ID, contact, address, discount, withholding, payment term/method, reference, bank details, clauses, notes) now shows a persistent uppercase caption; previously a filled value (`21`, `15`, `111`) lost its meaning when the placeholder vanished. Reuses existing `t.deals.*` keys — no i18n churn. Archivos: views/deals/QuoteBuilder.tsx, CHANGELOG.md.
- [2026-07-20 11:00 UTC] Back End + Front End: **Multiple contacts (stakeholders) per deal (#503, ships via Vercel; migration pending).** A deal kept its single primary contact (`deals.contact_id`) and gains a `deal_contacts` junction for ADDITIONAL stakeholders, each with an optional role (decision maker / champion / influencer / technical / procurement / end user / other). Migration `20260720120000` (org-scoped, CASCADE FKs, unique per deal+contact, RLS read=member / write=`jwt_can_write`, idempotent). `crmApi` nested `/deals/{id}/contacts[/{contactId}]` route (GET degrades to `[]` if the table isn't migrated yet, so the panel never breaks in the deploy window). New `DealContacts` card in the deal detail panel (pinned primary + add/remove stakeholders + per-row role, writes gated on `deals:update`). New `DealContact` type + stakeholder/role i18n across all 6 locales. Gate: typecheck 0 · i18n-lint OK · 2888 tests green (+10) · build 0. **PENDING human action:** apply the migration (`supabase db push`). Archivos: supabase/migrations/20260720120000_deal_contacts.sql, lib/supabase/crmApi.ts, components/deals/DealContacts.tsx, views/deals/DealsPage.tsx, types/index.ts, i18n/*, tests/lib/crmApiPaths.test.ts, tests/components/deals/DealContacts.test.tsx, CHANGELOG.md.
- [2026-07-20 12:30 UTC] Front End: **CrmMigration modal — remove the empty footer band + fix mapping formatting (ships via Vercel).** The "Migrate from another CRM" modal painted a bordered but empty footer band on the source-picker step (no Back button, no action) — dead space under the CRM tiles; the footer now only renders on steps that actually have controls. Also fixed the mapping-step "don't import" `<option>` (rendered with stray commas `, … ,` → `— … —`) and made the file summary meaningful (`file.csv · N contacts`). Gate: typecheck 0 · tests green (+2) · build 0. Archivos: components/import/CrmMigration.tsx, tests/components/import/CrmMigration.test.tsx, CHANGELOG.md.
- [2026-07-20 19:30 UTC] Front End + Back End: **Adversarial-review correctness sweep — Tickets + Deals (#515, ships via Vercel).** Findings from a Calendar/CustomFields/Tickets review, fixed the confirmed real ones: ticket status-badge counts now derive from the sprint-scoped set the list renders (badges no longer contradict visible rows under a sprint filter); `isOverdue` compares whole calendar days (dueAt is UTC-midnight of a date-only input) so a ticket due today isn't flagged overdue early for viewers at/behind UTC; a deactivated assignee stays selectable in the detail panel; a failed inline edit re-syncs with `fetchTickets({ silent: true })` instead of flashing the board to skeletons; and `dealsStore.updateQuote` reverts only the affected deal on failure (surgical rollback, matching moveDeal/updateDeal/reorderDeal). Gate: typecheck 0 · 2904 tests green · lint OK · build 0. Larger findings deferred with notes (calendar multi-day/overnight event editing; custom select/multiselect storing localized labels as values). Archivos: views/Tickets.tsx, store/ticketsStore.ts, store/dealsStore.ts, CHANGELOG.md.
- [2026-07-20 20:30 UTC] Back End + Front End: **Pricing Model v3 — per-seat + AI-usage metered, shipped DARK (#516, ships via Vercel; migration + edge deploy pending).** Adapts the founder's per-seat pricing spec to Propel's stack (Supabase + Deno edge + the existing config-driven `plans` table; no Prisma). Additive, idempotent, grandfathered — no entitlement/limit/billing behavior changes until later phases are wired and a rollout flag is flipped. Migration `20260720130000`: plans gain `billing_model`/`price_per_seat_*`/`min_seats`/`ai_credits_per_seat`/`ai_overage_per_credit`/`is_public`; subscriptions gain `billing_interval`/`seats`/`overage_*`/`unit_amount_override`/`feature_overrides`; organizations gain tax/VAT fields; seeds per-seat config on starter/growth/enterprise (recommended defaults, `[CONFIRM]`), hides Business as a grandfathered flat plan, backfills `subscriptions.seats`. `entitlements.ts` now returns `billingModel`/`seats`/`aiCreditsPerPeriod` and unions `feature_overrides` (flat/`{}` defaults reproduce today's behavior). Checkout `quantity = max(active members, min_seats)` (was `1`); webhook persists `seats` + `billing_interval`. `ChoosePlan` shows per-seat pricing + a seat picker + `is_public` filtering. Design + spec→Propel map: `docs/pricing-model-v3.md`. Gate: typecheck 0 · 2904 tests green (+6) · lint OK · build 0. **PENDING human action:** apply migration `20260720130000` FIRST, then deploy edge functions; `[CONFIRM]` prices + Spain VAT sign-off before charging. Follow-ups (documented): AI metering per-seat rewire + overage/report-only, Stripe metered overage + membership seat re-sync + `invoice.*` + Stripe Tax, BillingSection/PlanModal UI, acceptance tests. Archivos: supabase/migrations/20260720130000_pricing_model_v3_schema.sql, supabase/functions/_shared/entitlements.ts, supabase/functions/_shared/handlers/billing.ts, supabase/functions/stripe-webhook/index.ts, views/ChoosePlan.tsx, docs/pricing-model-v3.md, tests/entitlements.test.ts, tests/views/ChoosePlan.test.tsx, CHANGELOG.md.
- [2026-07-20 20:45 UTC] Back End + Front End: **Pricing v3 — AI metering per-seat + BillingSection UI + self-audit fixes (#518, ships via Vercel; still DARK).** Adversarial self-audit of the shipped v3 code fixed 3 real issues: the entitlements resolver falls back to the pre-v3 column SELECT if the v3 columns don't exist yet (edge-deployed-before-migration window) instead of throwing — which failed feature/limit guards OPEN and 500'd /billing/entitlements; `aiCreditsPerPeriod` is now `number|null` (null = unlimited, no longer coerced to a 0-credit cap); `feature_overrides` union masked to known FeatureKeys. Fase 2b: when `PRICING_V3_ENABLED=true` and the org is per-seat, the AI allowance = `ai_credits_per_seat × seats`, report-only by default (`AI_CREDIT_METERING_REPORT_ONLY`); flag off = zero change + zero extra lookup. Fase 4b: BillingSection acquisition grid shows per-seat pricing + seat picker + is_public filter (floored seats → checkout); active mode shows a seats + AI-credit line; `/billing/entitlements` returns billingModel/seats/aiCreditsPerPeriod. Gate: typecheck 0 · 2907 tests (+5) · lint OK · i18n OK · build 0. Archivos: supabase/functions/_shared/entitlements.ts, supabase/functions/_shared/handlers/ai.ts, supabase/functions/_shared/handlers/billing.ts, store/billingStore.ts, views/settings/BillingSection.tsx, tests/entitlements.test.ts, tests/views/settings/BillingSection.test.tsx, CHANGELOG.md.
- [2026-07-20 20:52 UTC] Back End: **Pricing v3 — dunning webhooks + membership seat re-sync + rollout flags (#519, edge; still DARK).** `stripe-webhook` handles `invoice.payment_failed`/`invoice.paid` by re-syncing the live subscription (past_due ↔ active) via the idempotent `syncSubscription`. `resyncSeats(orgId)` recomputes `subscriptions.seats = max(active members, plan.min_seats)` for per-seat plans on member add/activate/deactivate (best-effort, dark, never fails the member op; active members only per spec §4.2; checkout aligned). Rollout flags documented (`PRICING_V3_ENABLED` + `AI_CREDIT_METERING_REPORT_ONLY`). Deferred (Stripe-live + gestoría): metered AI-overage item, Stripe seat-quantity push, Stripe Tax + VIES. **PENDING human action:** deploy edge functions (after the migration). Archivos: supabase/functions/stripe-webhook/index.ts, supabase/functions/_shared/entitlements.ts, supabase/functions/_shared/handlers/members.ts, supabase/functions/_shared/handlers/billing.ts, docs/pricing-model-v3.md, CHANGELOG.md.
